# Module M — Stellar/Explosive Nucleosynthesis and Chemical-Return Proof

## Theorem M.1 — Exact triadic descent

Every admitted Module M carrier, reaction, event, yield, transport, dust, composition-return, and recurrence object descends from the sealed parent through the ordered specialization `CIF_M -> QV_M -> RFL_M` of the enhanced completed Module A kernel.

### Proof

`CIF_M` is defined only from the sealed L/E/C/D/K parent and inherited kernel. `QV_M` admits only witnessed transformations satisfying typed identity, transition, threshold, conservation, route, uncertainty, and ancestry conditions. `RFL_M` contains only states stabilized from those admitted transformations. No external generator appears in any map. Therefore every accepted M object has an explicit parent-to-triad-to-object derivation path. ∎

---

## Theorem M.2 — Stoichiometric conservation

Let `N` be the active stoichiometric matrix. For every exact conserved carrier vector `c` with `c^T N=0`, internal nuclear evolution preserves `c^T Y`.

### Proof

With `dY/dt=N J(Y,t)`,

\[
\frac d{dt}(c^{\mathsf T}Y)=c^{\mathsf T}N J=0.
\]

External photons, neutrinos, leptons, boundary fluxes, and escaped carriers are represented separately and enter the complete ledger with their signs. ∎

---

## Theorem M.3 — Forward/reverse rates cannot be independently tuned

For an admitted reversible route, forward and reverse fluxes are generated from conjugate matrix elements and the same state normalization; their thermal ratio is fixed by the reaction chemical affinity.

### Proof

The forward integrand contains `|<F|T|I>|^2`; the reverse contains `|<I|T^dagger|F>|^2`, which is the same squared amplitude under microreversibility. The ratio of occupation and blocking/stimulation factors in a thermal state is `exp(-beta Delta mu)`. Hence the rate ratio follows from the inherited microscopic and thermodynamic state. Independent retuning would violate the conjugate-route construction. ∎

---

## Theorem M.4 — Nonnegative-state invariance

The exact event-count reaction process preserves nonnegative carrier counts, and any admitted deterministic projection must preserve the nonnegative abundance cone.

### Proof

Every jump consumes only available integer reactants and produces nonnegative integer products. Thus the Markov jump state never leaves the nonnegative lattice. The deterministic law is admitted only through a positivity-preserving projection/integrator whose boundary flux for any absent species is nonnegative. A solver that exits the cone is obstructed. ∎

---

## Theorem M.5 — Nuclear energy closure

For a closed internal reaction set, the decrease in rest-mass energy plus binding change equals the increase in thermal, kinetic, photon, neutrino, and declared field/remnant channels within the event residual.

### Proof

Each route uses `Q_r=sum m_in-sum m_out` from the same carrier registry. Multiplying by the net extent and summing gives the total rest/binding release. The shared event packet assigns every released channel exactly once to matter, radiation, neutrinos, motion, remnant, or escape. Internal transfers cancel on summation, leaving only explicit boundary terms and the declared residual. ∎

---

## Theorem M.6 — Detailed-balance entropy production

For positive forward and reverse fluxes,

\[
\sigma=\sum_r(J_r^+-J_r^-)\log(J_r^+/J_r^-)\ge0.
\]

### Proof

For positive `x,y`, `(x-y) log(x/y)>=0` because `log` is strictly increasing. Summing the routewise inequalities proves the result. Equality holds exactly on the detailed-balance manifold for active routes. ∎

---

## Theorem M.7 — Equilibrium reductions share one parent law

NSE, QSE, Saha-like, and weak-equilibrium reductions are invariant/controlled manifolds of the full reversible network rather than independently fitted models.

### Proof

Each reduction sets selected fast affinities to zero and solves the corresponding nullspace constraints while retaining slow variables and conserved carriers. Because the constraints and reverse rates come from the same full network, the reduced state is embedded in the parent law. The overlap residual and reconstruction bound determine admissibility. ∎

---

## Theorem M.8 — Adaptive network completeness is componentwise

If every omitted-flux, energy, daughter, covariance, source-attribution, and composition-operator contribution is below its protected component tolerance, then the active truncation is complete at the declared scope; failure of any component blocks completion.

### Proof

The promotion rule evaluates each protected output separately against a scale and tolerance. Therefore no weighted average can cancel one failed component with unrelated successes. Stability under the next admissible refinement plus a bounded omitted remainder supplies the declared truncation certificate. ∎

---

## Theorem M.9 — Mixing-burning conservation

Coupled reaction and conservative zone transport preserve global isotope carriers and energy except for explicit stellar-boundary and event sources.

### Proof

Reaction terms preserve left-null carriers by Theorem M.2. Conservative zone fluxes telescope over internal interfaces. Paired mixing exchanges cancel between donor and recipient zones. Summing all zones leaves only stellar-boundary, transfer, ejecta, radiation, neutrino, and remnant terms already present in the shared event ledger. ∎

---

## Theorem M.10 — Event yield partition is exact

For every event and isotope, total processed material partitions into retained, fallback, and escaped carriers; net, radioactive, and dust-bearing yields are derived views and do not add independent mass.

### Proof

The event map partitions each final zone/parcel by its physical destination. These destination sets are disjoint and exhaustive, so their measures sum to the total. Net yield subtracts the typed initial carrier. Radioactive and dust-bearing labels select subsets or state views of the same partition and therefore cannot create extra mass. ∎

---

## Theorem M.11 — Radioactive daughter conservation and restart

For a complete daughter generator with `1^T Q_D=0`, total represented daughter mass is conserved and checkpoint/restart evolution equals direct evolution.

### Proof

`d(1^T y)/dt=1^T Q_D y=0`. The matrix exponential satisfies the semigroup identity `exp(Q(t2-t0))=exp(Q(t2-t1))exp(Q(t1-t0))`, proving restart equivalence. Escaped particles and energy are explicit boundary carriers. ∎

---

## Theorem M.12 — Isotope-conserving transport

The integral of each isotope density changes only through explicit event, decay, phase, and boundary fluxes.

### Proof

Integrate the conservative transport equation over the domain. The divergence term becomes a boundary flux. Internal phase-exchange terms appear with opposite signs for donor and recipient, and internal parcel/cell fluxes cancel. The remaining terms are exactly the declared sources and boundaries. ∎

---

## Theorem M.13 — Source ancestry remains normalized

If ancestry fractions are normalized initially and are transported with the same conservative operator as their isotope carrier, then the fractions remain normalized wherever the carrier is nonzero.

### Proof

Summing the ancestry-weighted transport equations over all sources returns the isotope transport equation because source fractions sum to one and source creation is assigned to exactly one normalized lineage mixture. Uniqueness of the conservative update preserves equality. ∎

---

## Theorem M.14 — Dust/gas phase exchange cannot create refractory mass

When dust formation and destruction are represented as antisymmetric transfers between typed gas/isotope and dust carriers, total represented refractory mass is invariant apart from explicit boundary/event sources.

### Proof

Every condensation/growth term enters dust positively and gas negatively; every destruction/return term enters with the opposite signs. Summation cancels internal exchange. Dust-bearing event yields remain subsets of the event material partition. ∎

---

## Theorem M.15 — Composition operators are conservative/passive at their declared domain

Composition-dependent opacity, emissivity, heating, cooling, ionization, and stress-energy operators exchange energy and momentum only through paired matter/radiation/field terms; passive absorption and dissipative forms are nonnegative.

### Proof

Each operator is a sum of typed carrier transition contributions. Absorption cross sections and dissipative quadratic forms are nonnegative by construction. Emission/absorption and matter/radiation four-force terms enter recipient equations with opposite signs. Summing the coupled system cancels internal exchange. ∎

---

## Theorem M.16 — Delay-time distributions are generated pushforwards

The source-class delay measure is determined by generated stellar/binary lineages and cannot be an independent empirical input.

### Proof

The birth measure is supplied by L. The witnessed evolution map assigns each lineage an event time or no-event branch. The pushforward under the delay map uniquely determines the delay measure. Replacing it with an empirical distribution would change the generated lineage map and violate parent immutability. ∎

---

## Theorem M.17 — Covariance remains positive semidefinite

If parent covariances and all process-noise terms are positive semidefinite, the propagated Module M covariance is positive semidefinite.

### Proof

For `Sigma_M=sum_i J_i Sigma_i J_i^T + sum_q Sigma_q` and any vector `v`, every term is a nonnegative quadratic form. Their sum is nonnegative. ∎

---

## Theorem M.18 — Replay locality

A Module M return can reopen only descendants of the earliest changed K/L interval and cannot alter Modules A–J.

### Proof

Every return includes changed-domain markers, event time, and dependency ancestry. The replay graph is the descendant subgraph of the earliest changed node. Frozen A–J nodes have no incoming dependency from M, so their state is invariant. ∎

---

## Theorem M.19 — K–L–M recurrence classification

Every instantiated recurrence branch is lawfully classifiable as fixed, periodic, attracting, branched, or explicitly nonconvergent from its return map and witnesses; no forced fixed point is required.

### Proof

A fixed point satisfies `R(X)=X`; local contraction follows from spectral radius below one in a compatible protected norm. A period-`p` cycle satisfies `R^p(X)=X` with lower periods excluded. An attracting set is certified by invariance and bounded attraction. Multiple witness-supported continuations form a branch family. Any branch failing these or closure conditions remains explicitly nonconvergent/obstructed. These categories exhaust the declared downstream statuses without artificial damping. ∎

---

## Theorem M.20 — Public-data firewall

No public yield, abundance pattern, reaction-rate fit, delay-time distribution, metallicity law, dust relation, or residual can alter a generated Module M state.

### Proof

The generating maps depend only on the sealed parent, inherited kernel, typed transition laws, and internally produced event/transport states. Benchmark objects are marked read-only and have no edge into `CIF_M`, `QV_M`, `RFL_M`, network promotion, branch selection, or K/L return. Therefore public comparison cannot back-propagate into generation. ∎

---

## Theorem M.21 — Exact Module N parent sufficiency

The completed Module M packet supplies every nuclear/chemical field required by the KLM→N handoff without forcing Module N to reconstruct missing isotope, yield, decay, transport, dust, or composition physics.

### Proof

The packet contains the full isotope/nuclear registries, yields, radioactive carriers, phases, dust/molecules, enrichment and source histories, host/cosmic fields, composition operators, closure state, covariance, memory, ancestry, and restart. These match the named M contribution in the sealed handoff. ∎

---

## Theorem M.22 — Module M completion

Under the declared finite representation, parent regularity, typed registry, route-admission, positivity, conservation, convergence, and recurrence-classification conditions, Module M is complete and frozen at derivational scope and Module N is the sole next active child.

### Proof

Theorems M.1–M.21 establish triadic descent; carrier/reaction constitution; forward/reverse closure; positivity; mass, charge, energy, yield, daughter, transport, phase, and source-ancestry closure; adaptive completeness; composition return; covariance; replay locality; recurrence classification; public-data isolation; and exact child sufficiency. These are the complete obligations named by the Module M plan at constitutive/finite-verification scope. Empirical validation and manifested-universe assembly remain explicitly downstream. ∎
