# Module J — Detailed Scientific Repair Plan

## Primordial Covariance, Linear Spectra, Growth, and Nonlinear-Ready Fields

## 1. Basis and scope

This plan is built from:

1. `architecture/2RFC_DETAILED_SCIENTIFIC_GAP_TO_LIBRARY_REPAIR_PLAN.md` as the primary repair spine;
2. `modules/H/MODULE_H_DETAILED_SCIENTIFIC_REPAIR_PLAN.md` as the frozen linear-operator parent plan;
3. `modules/H/MODULE_H_UNIT_TO_I_SCIENTIFIC_HANDOFF.md` as the protected Hᵁ→I boundary;
4. `modules/I/MODULE_I_DETAILED_SCIENTIFIC_REPAIR_PLAN.md` as the realized-background parent plan;
5. `modules/I/MODULE_I_TO_H_INSTANTIATED_SCIENTIFIC_HANDOFF.md` as the immutable I→H[I] instantiation boundary;
6. `Module_J_Individual_Enhancement_Plan.md` as the exact individualized statement of Module J science;
7. the Module K individualized plan as the nonlinear-entry boundary;
8. applicable Presentation 29 and Presentation 30 primordial, covariance, transfer, growth, and realization science;
9. Module A route, branch, memory, and no-loss grammar only where physically mapped to primordial modes and covariance.

The old Module J run sequence, source-register bureaucracy, proof-obligation ledger, gate taxonomy, certificate architecture, repository machinery, and lifecycle system are not adopted.

The module is the unit of scientific repair.

The protected dependency is:

```text
G -> Hᵁ -> I -> H[I] -> J -> K
```

Module J begins from two immutable direct parents:

```text
H[I] basis-normalized linear responses
+
Module I realized background
```

It ends with:

```text
internally derived primordial covariance
+
realized linear spectra and growth
+
nonlinear-ready finite-volume fields
+
physical linearity boundary
```

---

# 2. Canonical scientific responsibility

Module J must derive RFC's primordial mode covariance and convert the frozen H[I] response basis into the realized linear radiative, matter, metric, neutrino, compression-relic, dissipative-tail, and cross-sector state of the universe.

Its central theorem target is:

> Given the immutable H[I] transfer responses, the realized Module I geometry and sector evolution, the Module A triadic relational constitution, the Module B Big-Implosion common ancestry, and the complete upstream physical history, RFC derives an admissible primordial mode covariance and contracts it with the frozen responses to produce the universe's linear spectra, cross-spectra, growth histories, projected source kernels, and nonlinear-ready initial fields. The construction preserves independent mode and route identity, satisfies covariance positivity, gauge and conservation closure, propagates uncertainty, identifies the physical boundary of linear validity, and exports a complete restartable state to Module K without empirical tuning.

The direct claim is:

```text
RFC derives its primordial covariance,
realized linear spectra,
and matter-growth history
from the Big-Implosion state and triadic relational law.
```

Module J is not:

- a fitted primordial power law;
- an observed CMB-spectrum matching module;
- a growth-normalization wrapper;
- a single total-matter spectrum;
- a public Boltzmann-code output collector;
- a nonlinear halo or galaxy module;
- a survey, instrument, foreground, or likelihood module.

Its proof object is the complete covariance-to-transfer-to-spectrum-to-field construction.

---

# 3. Exact direct parents

Module J consumes:

```text
P_J,in = (P_H[I]->J, P_I->J)
```

## 3.1 Instantiated transfer parent

The H[I] packet must include:

- frozen operator and background identities;
- instantiation identity and domain-compliance result;
- complete primordial mode basis and normalization;
- scalar, vector, tensor, parity, and helicity labels;
- direct-hierarchy responses;
- line-of-sight responses;
- photon temperature and polarization responses;
- baryon, ordinary-matter, compression-relic, dissipative-tail, neutrino, radiation, and active-field responses;
- metric, Weyl, growth, and projected-source responses;
- collision, switch, and approximation histories;
- Green functions;
- response Jacobians;
- transfer covariance and numerical-error maps;
- gauge, conservation, constraint, branch, pathology, memory, ancestry, and restart state.

## 3.2 Realized background parent

The Module I packet must include:

- realized geometry, topology, clocks, and coordinates;
- expansion and acceleration histories;
- ordinary, radiative, relic, compression-relic, dissipative-tail, field, and defect backgrounds;
- inter-sector transfer currents;
- curvature, anisotropy, averaging, and backreaction status;
- event, horizon, redshift, null-propagation, and distance histories;
- background covariance and derivative operators;
- intrinsic signatures, branch identity, memory, ancestry, and restart state.

Module J may not reconstruct either parent.

---

# 4. Where the current repository misses the mark

Module J is scientifically absent from the current 2-RFC branch.

The repository therefore lacks:

- an internally generated primordial mode covariance;
- mode amplitudes and cross-correlations;
- scalar/vector/tensor covariance blocks;
- symmetry and statistical-status derivation;
- covariance positivity and finite-power proof;
- transfer-covariance contraction;
- primary linear CMB temperature and polarization spectra;
- full multi-species and cross-sector spectrum matrices;
- scale- and time-dependent growth histories;
- direct ensemble versus transfer reconstruction;
- finite-volume initial-field construction;
- a physical linearity boundary;
- a complete Module K parent state.

Any attempt to jump directly from a transfer operator to a plotted spectrum would omit the central scientific content owned by Module J.

---

# 5. Triadic realization

Module J must make the triad operational.

## 5.1 CIF_J

`CIF_J` is the admitted primordial capacity:

- lawful initial modes;
- route classes;
- sector-relative modes;
- scalar, vector, and tensor support;
- parity and helicity capacity;
- cross-correlation capacity;
- recursive-depth and memory support;
- deterministic branch variables and stochastic realization degrees of freedom.

## 5.2 QV_J

`QV_J` is the qualification and transformation action that:

- enforces regularity and conservation;
- assigns or rejects covariance support;
- enforces Hermiticity and positive semidefiniteness;
- applies symmetry and reality conditions;
- preserves independent mode and route identities;
- contracts primordial covariance with H[I] responses;
- propagates uncertainty;
- witnesses the linearity boundary;
- constructs consistent finite-volume fields.

## 5.3 RFL_J

`RFL_J` is the stabilized linear universe:

```text
primordial covariance
+ realized spectra
+ growth histories
+ projected source kernels
+ nonlinear-ready fields
+ uncertainty
+ memory
+ ancestry
```

The module action is:

```text
CIF_J -> QV_J -> RFL_J
```

Randomness may enter only through explicitly declared realization variables. It may not replace a missing physical derivation.

---

# 6. Primordial mode-basis constitution

Module J must inherit and freeze the complete H[I] mode basis before assigning amplitudes.

Each mode must carry:

- identifier;
- physical interpretation;
- scalar, vector, or tensor type;
- parity and helicity where active;
- species and sector support;
- gauge behavior;
- regularity and superhorizon behavior;
- normalization;
- source, route, branch, event, and memory ancestry;
- independence or lawful quotient-equivalence witness;
- allowed cross-correlation partners.

The basis may include, where derived:

- total-curvature-like or adiabatic modes;
- baryon, neutrino, compression-relic, dissipative-tail, and other entropy modes;
- compensated density modes;
- relative-velocity modes;
- vector and vortical modes;
- tensor and helicity modes;
- magnetic or defect modes;
- RFC-specific route-memory modes.

No mode may be removed solely because it is expected to have small amplitude.

---

# 7. Random variables, phases, and statistical status

For each admitted mode `A`, define:

```text
q_A(k)
```

or its lawful inhomogeneous generalization.

Module J must distinguish:

- deterministic branch variables;
- stochastic amplitudes;
- stochastic phases;
- ensemble distributions;
- physical route multiplicity;
- realization seeds;
- numerical sampling noise.

The probability law must state:

- mean;
- covariance;
- support;
- phase law;
- reality condition;
- parity and helicity status;
- homogeneity and isotropy status;
- Gaussian or non-Gaussian status;
- higher-moment status;
- mode-coupling restrictions.

Statistical homogeneity, isotropy, Gaussianity, parity, and scale invariance must be derived, bounded, approximate, broken, or absent. None may be silently presumed.

---

# 8. Triadic primordial covariance generator

The central generator is:

```text
C_J : (A-I ancestry, H[I] mode basis) -> P_AB(k,k')
```

with:

```text
P_AB(k,k') = < q_A(k) q_B*(k') >
```

and, only when statistically homogeneous:

```text
<q_A(k)q_B*(k')>
=
(2pi)^3 delta_D(k-k') P_AB(k)
```

The generator must define:

- how CIF admits mode and correlation capacity;
- how QV weights, correlates, suppresses, or rejects routes;
- how RFL stabilizes the accepted covariance;
- how common Big-Implosion ancestry constrains sector covariance;
- how recursive depth and memory enter spectral weighting;
- how dimensional scales arise;
- how branch selection is witnessed;
- how uncertainty propagates into covariance.

The generator must be deterministic conditional on the declared physical source packet, except for declared realization randomness.

---

# 9. Covariance algebra and physical validity

For every scale and branch, the covariance must satisfy:

```text
P_AB = P_BA*
```

and for every complex vector `v`:

```text
v^dagger P v >= 0
```

The program must establish:

- Hermiticity;
- positive semidefiniteness;
- dimensional consistency;
- finite infrared and ultraviolet power over the admitted domain;
- correct reality conditions;
- cross-spectrum symmetry;
- stable rank and eigenstructure;
- lawful zero eigenmodes and degeneracies;
- branch-specific covariance where required.

Negative covariance eigenvalues or nonintegrable power are obstructions, not targets for clipping without a physical derivation.

---

# 10. Scalar, vector, tensor, parity, and helicity blocks

Organize the covariance as:

```text
P = [[P_SS,P_SV,P_ST],
     [P_VS,P_VV,P_VT],
     [P_TS,P_TV,P_TT]]
```

Module J must determine which blocks are:

- required;
- forbidden by derived symmetry;
- zero by derivation;
- nonzero through route, branch, defect, magnetic, or memory structure;
- negligible only within a declared approximation.

Where active, derive:

- tensor power and polarization;
- vector power and sourcing/decay;
- left-right helicity covariance;
- parity-even and parity-odd spectra;
- scalar-tensor, scalar-vector, and vector-tensor correlations.

No block may be forced to zero merely because a benchmark model commonly sets it to zero.

---

# 11. Adiabatic, entropy, compensated, and relative-sector modes

Module J must track:

- total-curvature-like modes;
- relative-density modes;
- relative-velocity modes;
- compensated modes;
- baryon-photon relative modes;
- neutrino density and velocity modes;
- compression-relic versus ordinary-matter modes;
- dissipative-tail versus total-density modes;
- RFC-specific recursive-memory modes.

Each mode must have:

- a transfer response;
- covariance amplitude;
- phase and correlation rule;
- regularity and conservation witness;
- physical interpretation;
- ancestry;
- downstream significance.

---

# 12. Higher-order statistics boundary

Module J's core theorem is two-point covariance plus linear evolution.

It must nevertheless declare the status of:

- bispectrum;
- trispectrum;
- phase coupling;
- intermittency;
- non-Gaussian tails;
- route-count fluctuations.

If higher-order statistics are derived and required for Module K initial conditions, they must be exported with full normalization and sampling rules.

Otherwise they must remain explicitly unresolved. Gaussianity may not be silently assumed.

---

# 13. Transfer-covariance contraction

For species or sectors `a,b`:

```text
P_ab(k,t)
=
sum_AB T_aA(k,t) P_AB(k) T_bB*(k,t)
```

For projected radiative observables `X,Y`:

```text
C_l^XY
=
4pi integral dlnk sum_AB
P_AB(k) Delta_lA^X(k) Delta_lB^{Y*}(k)
```

The contraction must preserve:

- mode indices;
- route identity;
- complex phases;
- helicity and parity;
- cross-correlations;
- species and sector identity;
- gauge-invariant observable definitions;
- integration measure;
- dimensional normalization;
- transfer and covariance uncertainty;
- branch and memory ancestry.

The H[I] responses may not be structurally modified during contraction.

---

# 14. Primary linear CMB spectra

Module J must generate intrinsic pre-instrument primary spectra for all admitted components, including where active:

```text
C_l^TT
C_l^TE
C_l^EE
C_l^BB
C_l^TB
C_l^EB
```

The output must preserve separate contributions from:

- last-scattering sources;
- Doppler sources;
- metric and integrated linear sources;
- scalar, vector, and tensor modes;
- compression-relic and dissipative-tail perturbations;
- parity and helicity sectors;
- route and branch classes.

Module J may produce unlensed primary spectra and linear lensing-source products.

It does not own nonlinear lensing, late astrophysical reionization, foregrounds, beams, noise, masks, or likelihoods.

---

# 15. Full matter, radiation, metric, and sector spectrum matrix

Module J must generate:

```text
P_matrix(k,t) = [P_ab(k,t)]
```

including:

- total matter;
- baryons;
- compression relic;
- dissipative tail where perturbatively active;
- neutrinos and other free-streaming species;
- radiation;
- density;
- velocity divergence;
- momentum density;
- pressure and anisotropic stress;
- metric potentials;
- Weyl potential;
- all material cross-spectra.

A single total-matter spectrum is insufficient.

Every spectrum must declare:

- gauge or gauge-invariant status;
- dimensions and normalization;
- time and scale domain;
- covariance and uncertainty;
- mode and route contribution structure;
- ancestry.

---

# 16. Linear growth and growth-rate histories

For every admitted species or eigenmode, derive:

```text
D_a(k,t)
```

and:

```text
f_a(k,t) = d ln D_a / d ln a
```

The module must identify:

- scale-independent regimes;
- scale-dependent growth;
- radiation suppression;
- free-streaming suppression;
- compression-relic clustering transitions;
- dissipative-tail drag or enhancement;
- metric-slip effects;
- equality and acceleration transitions;
- decaying and growing eigenmodes;
- memory-bearing transients.

Growth normalization must arise from the generated primordial covariance. It may not be imposed externally.

---

# 17. Acoustic, damping, and free-streaming signatures

Module J must preserve the physical ancestry of:

- sound-horizon scale;
- acoustic phase;
- baryon loading;
- photon diffusion damping;
- drag release;
- neutrino phase shift;
- free-streaming cutoffs;
- compression-relic clustering scale;
- dissipative-tail transition scale;
- magnetic or defect features where admitted;
- route-memory spectral features.

These are generated signatures, not adjustable curve features.

---

# 18. Metric, Weyl, and projected linear source histories

Module J must generate:

- gauge-invariant metric-potential spectra;
- metric slip;
- potential growth and decay;
- Weyl-potential spectra;
- integrated derivative source histories;
- linear lensing-potential sources;
- redshift-space velocity responses;
- number-density and volume-distortion source terms;
- standard-ruler, clock, and siren perturbation kernels;
- gravitational-wave propagation responses where active.

These are linear source kernels, not final nonlinear or survey observables.

---

# 19. Gauge and representation consistency

Module J must verify:

- gauge transformation consistency;
- agreement of gauge-invariant observables across independent formulations;
- superhorizon cancellation where required;
- removal of pure-gauge modes;
- consistency of species-field exports;
- consistency between continuum and finite-volume representations;
- consistency between direct and transfer-based observables.

A gauge-dependent spectrum may be exported only with its gauge declaration and transformation map.

---

# 20. Conservation and constraint closure

The realized spectra and fields must satisfy, within declared scale- and time-dependent tolerance:

- total stress-energy conservation;
- species continuity and momentum equations;
- geometric constraints;
- compensation conditions;
- covariance normalization;
- cross-spectrum symmetry;
- reality conditions;
- transfer/direct agreement;
- field-level constraint closure.

Residuals must remain functions of scale, time, mode, and species.

No aggregate score may hide a failed mandatory condition.

---

# 21. Direct ensemble evolution versus transfer reconstruction

Module J must implement at least two physically independent routes:

1. covariance contraction with frozen H[I] basis responses;
2. direct evolution of a representative ensemble of primordial field realizations through the same frozen operator.

The routes must agree for:

- auto-spectra;
- cross-spectra;
- mode decomposition;
- phase evolution;
- growth histories;
- projected linear spectra;
- uncertainty estimates.

Disagreement beyond channel-specific tolerance is a module failure.

---

# 22. Finite-volume realization generator

Module J must construct finite-volume initial fields:

```text
X_J(x,t_init) = (
  delta_a,
  theta_a,
  sigma_a,
  Phi,
  Psi,
  v_a,
  sector memory,
  ancestry
)
```

The generator must specify:

- Fourier amplitudes consistent with `P_AB`;
- cross-species and cross-mode phase consistency;
- reality conditions;
- gauge and constraint consistency;
- random-seed and phase identity;
- box size and geometry;
- mesh or particle representation;
- aliasing control;
- long-mode and missing-mode treatment;
- paired/fixed or other variance-control ensembles where lawful;
- continuum-to-discrete transfer;
- uncertainty;
- route, branch, sector, and memory ancestry.

The generated field ensemble must recover the target covariance within declared finite-volume uncertainty.

---

# 23. Ensemble and uncertainty constitution

Module J must distinguish:

```text
Sigma_total =
  Sigma_primordial
  + Sigma_transfer
  + Sigma_background
  + Sigma_numeric
  + Sigma_sampling
  + Sigma_finite_volume
  + Sigma_cosmic_variance
  + Sigma_model
```

The program must define:

- ensemble size;
- phase pairing;
- fixed-amplitude options;
- unbiased estimators;
- covariance-estimator uncertainty;
- seed reproducibility;
- cross-observable covariance;
- conditions for using a single realization downstream.

Cosmic variance is not permission to ignore model or numerical error.

A schematic propagation is:

```text
Sigma_J =
  J_prim Sigma_prim J_prim^T
  + J_H Sigma_H[I] J_H^T
  + J_I Sigma_I J_I^T
  + Sigma_numeric
  + Sigma_sampling
  + Sigma_model
```

---

# 24. Identifiability, degeneracy, and eigenmode analysis

Module J must determine which primordial and sector combinations are physically distinguishable.

The analysis must include:

- covariance eigenvalues and eigenvectors;
- transfer null spaces;
- near-degenerate mode combinations;
- scale-localized identifiability;
- observable-space principal components;
- route-memory separability;
- sensitivity to background and transfer uncertainty;
- downstream observables required to break degeneracies.

A weak observable signature does not justify erasing an independent physical mode.

---

# 25. Physical linearity boundary and nonlinear promotion surface

Module J must derive a domain:

```text
D_lin = {(k,t,a,sector,route): epsilon_lin <= epsilon_max}
```

from witnesses such as:

- perturbation amplitude;
- variance per logarithmic interval;
- second-order correction estimates;
- mode-coupling strength;
- Lagrangian displacement variance;
- shell-crossing risk;
- stress-closure failure;
- transfer/direct disagreement;
- conservation or constraint degradation.

The boundary may depend on:

- scale;
- time or redshift;
- species;
- sector;
- route;
- required downstream accuracy.

No single conventional nonlinear cutoff may replace the witnessed domain map.

---

# 26. Module K nonlinear-ready parent state

Module J must export a complete restartable state:

```text
P_J->K = (
  realized background and clocks,
  H[I] transfer identities,
  primordial covariance and statistics,
  complete linear spectrum matrix,
  growth and response histories,
  finite-volume fields,
  positions or mesh variables,
  velocities and momenta,
  species and sector masses or weights,
  metric and potential fields,
  radiation and neutrino representations,
  compression-relic perturbation state,
  dissipative-tail perturbation state,
  thermal and stress state,
  long-range constraint solution,
  random-seed and phase identity,
  linearity domain and promotion surfaces,
  ensemble identifiers,
  uncertainty and covariance,
  mode, route, branch, event, source, and memory ancestry,
  restartable physical state
)
```

Module K must not infer missing phase, normalization, gauge, species, stress, covariance, or ancestry information.

---

# 27. Accepted-physics limits

Where claimed, Module J must recover lawful limits such as:

- single adiabatic scalar mode;
- diagonal covariance;
- uncoupled species;
- no-interaction dark sectors;
- radiation- and matter-dominated growth limits;
- scale-independent growth where physically applicable;
- analytic projection systems;
- simple tensor or vector limits;
- Gaussian homogeneous-isotropic limits when derived.

Limit recovery is verification, not import of fitted public values.

---

# 28. Numerical convergence and independent formulation

The scientific result must converge under independent variation of:

- wave-number range and sampling;
- angular-multipole range;
- time and scale sampling;
- covariance-basis resolution;
- recursive-depth truncation;
- integration rules and tolerances;
- response interpolation order;
- numerical precision;
- ensemble size;
- simulation volume;
- mesh or particle resolution;
- long-mode treatment;
- linearity-boundary estimator.

Required outputs include:

- scale- and time-resolved convergence maps;
- covariance-eigenvalue stability;
- direct-versus-transfer residuals;
- spectrum and growth stability;
- finite-volume recovery;
- uncertainty stability.

At least two materially independent formulations must reproduce the core result, such as:

- covariance contraction versus direct ensemble evolution;
- eigendecomposition versus Cholesky or other valid factorization;
- Fourier versus configuration-space reconstruction;
- independent projection quadrature;
- independent realization generator.

---

# 29. Stability, causality, and pathology boundaries

Module J must detect and classify:

- negative covariance eigenvalues;
- divergent infrared or ultraviolet power;
- nonintegrable projection kernels;
- unstable transfer amplification;
- gauge singularities;
- superluminal or acausal inherited propagation;
- spurious parity or helicity artifacts;
- aliasing and finite-volume contamination;
- discontinuous transfer/background interpolation;
- seed collisions;
- ill-conditioned covariance factorization;
- negative or nonphysical species spectra;
- artificial growth from numerical normalization;
- false nonlinear promotion from numerical error.

Pathological branches are obstructed rather than tuned.

---

# 30. Public-data firewall

Before any public comparison, freeze:

- H[I] responses;
- Module I background;
- mode basis;
- covariance generator;
- normalization conventions;
- phase and seed rules;
- statistical assumptions;
- spectrum definitions;
- growth definitions;
- linearity boundary;
- uncertainty model;
- intrinsic signatures and falsifiers.

Public data may not determine:

- covariance amplitude;
- tilt, running, features, or cutoffs;
- mode mixture;
- cross-correlation;
- tensor or vector content;
- dark-sector clustering;
- growth normalization;
- branch choice;
- nonlinear initialization;
- uncertainty widths.

Empirical comparison belongs to Module P after the universe is frozen in Module O.

---

# 31. Intrinsic signature registry

Before public comparison, Module J must freeze signatures with:

- physical origin;
- triadic origin;
- mode and route ancestry;
- predicted sign;
- scale and time dependence;
- uncertainty;
- robustness to numerical refinement;
- robustness to physically meaningful ablations;
- downstream observation pathway;
- explicit falsifier.

Possible signature classes include:

- nonstandard covariance rank or cross-mode structure;
- route-memory correlations;
- compression-relic clustering transitions;
- dissipative-tail growth or metric-slip structure;
- parity or helicity signals;
- distinctive acoustic, damping, or free-streaming features;
- branch-dependent growth or projected-source structure.

No signature may be invented after observing a mismatch.

---

# 32. Logical scientific repair order

This is not a run sequence.

1. Freeze H[I] and Module I parent packets.
2. Freeze the complete primordial mode basis and normalization.
3. Define primordial random variables, phases, support, and statistical status.
4. Construct the triadic covariance generator.
5. Prove Hermiticity, positive semidefiniteness, dimensions, and finite power.
6. Resolve scalar, vector, tensor, entropy, compensated, dark-sector, and cross-mode blocks.
7. Derive spectral shapes, scales, features, cutoffs, and cross-correlations.
8. Contract covariance with H[I] responses.
9. Independently evolve a representative field ensemble.
10. Generate primary linear CMB spectra.
11. Generate full matter, radiation, neutrino, metric, and dark-sector spectra and cross-spectra.
12. Generate scale- and time-dependent growth histories.
13. Generate linear projection-source kernels.
14. Verify gauge, conservation, constraints, and direct/transfer agreement.
15. Build finite-volume field realizations.
16. Derive the physical linearity and nonlinear-promotion boundary.
17. Propagate uncertainty and cross-observable covariance.
18. Establish numerical convergence and independent reproduction.
19. Freeze intrinsic signatures and falsifiers.
20. Assemble and independently restart the Module K parent state.

---

# 33. Module J completion statement

The repaired Module J should support this direct statement:

> From the immutable H[I] basis responses and the realized Module I background, RFC derives a complete primordial mode covariance from triadic and Big-Implosion ancestry, proves its Hermiticity, positive semidefiniteness, dimensional validity, and finite power, resolves all admitted scalar, vector, tensor, entropy, compensated, dark-sector, parity, helicity, and cross-mode blocks, contracts that covariance with the frozen transfer operator, and generates the realized primary linear CMB, matter, radiation, neutrino, metric, Weyl, compression-relic, dissipative-tail, and cross-sector spectra and growth histories. It independently reproduces those results through ensemble evolution, constructs gauge- and constraint-consistent finite-volume fields, derives the physical boundary of linear validity, propagates complete uncertainty, and exports a restartable nonlinear-ready state to Module K without public-data fitting or retuning of H[I] or Module I.

The module must simultaneously state what it does not yet prove:

- nonlinear collapse and shell crossing;
- halos and merger histories;
- nonlinear lensing closure;
- gas cooling, stars, feedback, and enrichment;
- galaxies and observer-ready records;
- empirical agreement;
- terminal Big-Rip physics.

Those belong to Modules K-Q.

Module J is complete only when Module K can restart without inventing any missing phase, normalization, species, sector, stress, covariance, gauge, linearity, uncertainty, memory, ancestry, or field information.