# Module I → Module H[I] Scientific Handoff

## Purpose

This file seals the immutable instantiation boundary between:

- the frozen background-parameterized linear operator constructed in Module Hᵁ;
- the realized RFC background constructed in Module I;
- the background-instantiated response operator H[I];
- the primordial covariance and linear-realization work owned by Module J.

H[I] is not a second transfer module and not a place to revise Module H after seeing the realized background. It is the lawful evaluation of a frozen operator on a realized background that has already been proved to lie inside the operator's admissible domain.

This handoff defines:

- the two immutable parent packets required by H[I];
- the exact instantiation map;
- the operations that are lawful during instantiation;
- the scientific structures that remain frozen;
- the response products H[I] must generate;
- the conservation, gauge, composition, uncertainty, and branch conditions that must remain closed;
- the exact H[I] state delivered to Module J.

It does not import the former run system, gate taxonomy, source-register bureaucracy, certificate architecture, repository lifecycle machinery, or acceptance paperwork.

---

# 1. Protected dependency order

The scientific order is:

```text
Module G
-> Module Hᵁ
-> Module I
-> Module H[I]
-> Module J
```

The scientific meaning is:

```text
Hᵁ:
construct and freeze the universal background-parameterized
linear operator, mode basis, gauges, collisions,
hierarchies, source grammar, closures, and domain D_B

I:
derive the realized RFC geometry, clocks, expansion,
sector histories, horizons, distances, and covariance B_I

H[I]:
instantiate the frozen Hᵁ operator on B_I
and generate basis-normalized response histories

J:
derive primordial mode covariance and contract it
with the frozen H[I] responses
```

Therefore:

```text
H[I] != operator redesign
H[I] != background fitting
H[I] != primordial covariance generation
H[I] != observed-spectrum reconstruction
H[I] != nonlinear evolution
```

---

# 2. Dual immutable parent state

H[I] begins from:

```text
P_H[I],in = (P_Hᵁ^op, P_I^bg)
```

where:

- `P_Hᵁ^op` is the frozen operator packet from Module Hᵁ;
- `P_I^bg` is the realized background packet from Module I.

Neither parent may be reconstructed inside H[I].

## 2.1 Frozen operator parent

The Hᵁ parent includes:

```text
P_Hᵁ^op = (
  B_RFC[B],
  D_B,
  X_H,
  Mode_H,
  Gauge_H,
  Constraint_H,
  Conservation_H,
  Species_H,
  Sector_H,
  Collision_H,
  Hierarchy_H,
  SourceGrammar_H,
  InitialSeries_H,
  StiffRegime_H,
  Truncation_H,
  DirectSolver_H,
  LOSGrammar_H,
  GreenSchema_H,
  DerivativeSchema_H,
  UncertaintyOperator_H,
  BackgroundInsertionMap_H,
  BackgroundSampling_H,
  PathologyExclusions_H,
  IntrinsicSignature_H,
  Memory_H,
  Ancestry_H,
  FrozenOperatorIdentity_H
)
```

## 2.2 Realized background parent

The Module I parent includes:

```text
P_I^bg = (
  BackgroundIdentity_I,
  B_I,
  VariableMap_I,
  UnitMap_I,
  Clock_I,
  Coordinate_I,
  Geometry_I,
  Curvature_I,
  Topology_I,
  Scale_I,
  Expansion_I,
  Sector_I,
  Transfer_I,
  RecombinationBackground_I,
  Event_I,
  Horizon_I,
  Redshift_I,
  Distance_I,
  Sampling_I,
  Derivative_I,
  Sigma_I,
  Branch_I,
  Pathology_I,
  Signature_I,
  Memory_I,
  Ancestry_I,
  Restart_I
)
```

---

# 3. Admission condition

Instantiation is lawful only if Module I proves:

```text
B_I in D_B
```

The proof must establish:

- complete variable coverage;
- unit and dimensional consistency;
- species and sector identity consistency;
- coordinate and time-map consistency;
- continuity and differentiability required by Hᵁ;
- valid treatment of event surfaces and nonanalytic points;
- boundedness and positivity where required;
- required first and higher derivatives;
- lawful interpolation and sampling density;
- compliance with all Hᵁ pathology exclusions;
- overlap consistency with the Module G physical parent;
- complete covariance and branch attachment.

If the proof fails, H[I] is obstructed.

The scientific response is not to modify Hᵁ or retune Module I until the pair fits. The physical defect must be located in the responsible module or retained as an obstruction.

---

# 4. Exact instantiation map

The only lawful operation is:

```text
B_H[I] = Instantiate(B_RFC, B_I)
```

At the coefficient level, the map replaces typed background placeholders with the realized histories from Module I:

```text
C_H[B] -> C_H[B_I]
```

for all required coefficients, including where active:

- scale and expansion histories;
- physical and conformal clocks;
- metric, connection, curvature, and topology histories;
- ordinary-sector density, pressure, sound, stress, and transfer histories;
- radiative and relic-sector histories;
- compression-relic background law;
- dissipative-tail background law;
- neutrino and free-streaming histories;
- recombination, opacity, drag, and visibility background functions;
- collision and interaction-rate histories;
- horizon and event coordinates;
- dimensional normalization maps;
- derivative and sensitivity histories;
- background covariance.

The insertion map must be deterministic conditional on the two parent packets.

---

# 5. Lawful instantiation operations

H[I] may:

- populate frozen coefficient functions with Module I histories;
- apply the frozen variable and unit maps;
- evaluate frozen equations on the realized background;
- construct lawful interpolants under Hᵁ's predeclared error rules;
- place frozen event and approximation switches at Module I event surfaces;
- refine numerical sampling under already frozen convergence criteria;
- propagate Module I covariance through the frozen response operator;
- classify lawful background branches separately;
- identify a domain obstruction or numerical obstruction;
- generate direct-hierarchy, Green-function, and line-of-sight responses.

Numerical refinement may improve representation of the frozen equations. It may not change the scientific equations.

---

# 6. Frozen structures that may not change

H[I] may not alter:

- perturbation-state dimension;
- species and sector identities;
- scalar, vector, and tensor decomposition;
- helicity and parity conventions;
- gauge definitions and gauge transformations;
- gauge-invariant variable definitions;
- geometric and species constraint grammar;
- conservation laws;
- collision and momentum-transfer forms;
- photon intensity hierarchy;
- photon polarization hierarchy;
- neutrino and free-streaming hierarchies;
- compression-relic or dissipative-tail perturbation grammar;
- initial-mode basis;
- initial-mode normalization;
- superhorizon regularity conditions;
- tight-coupling and stiff-regime equations;
- hierarchy truncation and closure laws;
- direct-solution grammar;
- line-of-sight source grammar;
- Green-function definitions;
- response-derivative definitions;
- uncertainty decomposition;
- admissible-domain criteria;
- scientific tolerances and falsifiers;
- intrinsic signature definitions.

H[I] also may not alter Module I's realized background, sector histories, or branch identity.

---

# 7. Forbidden inputs and substitutions

H[I] may not use:

- public transfer functions;
- public CMB or matter spectra;
- a public best-fit background;
- a public recombination or reionization table;
- an observed acoustic scale;
- a fitted growth normalization;
- a fitted primordial amplitude, tilt, running, tensor fraction, or mode mixture;
- post-hoc response rescaling;
- public likelihood gradients;
- a preferred branch selected from observational similarity;
- hidden changes to initial-mode normalization;
- hidden removal of inconvenient independent modes;
- a public Boltzmann code output as a missing internal response.

Benchmark equations and solutions may be used only after the RFC response is frozen and only as declared verification objects.

---

# 8. Basis-normalized response constitution

For every admitted primordial basis mode `A`, H[I] must generate a unit response:

```text
T_A(k,eta) = B_H[I] e_A
```

where `e_A` is the frozen normalized initial basis vector.

The response state must retain:

- mode identity;
- route and branch ancestry;
- scalar/vector/tensor class;
- helicity and parity;
- species and sector support;
- gauge representation and gauge-invariant reconstruction;
- phase and normalization convention;
- collision and approximation history;
- background coefficient ancestry;
- uncertainty and covariance.

Independent responses may not be merged because they appear similar in one projection.

---

# 9. Required transfer products

H[I] must generate, where physically active:

## 9.1 Species and sector responses

```text
T_{aA}(k,eta)
```

for:

- baryon density, velocity, pressure, and stress;
- total ordinary matter;
- compression-relic density, velocity, pressure, sound, and stress;
- dissipative-tail density, velocity, pressure, sound, and stress;
- photons;
- neutrinos and other free-streaming species;
- ionization-facing and residual charged components where linearly active;
- magnetic, defect, vortical, or other generated components where active.

## 9.2 Metric and geometric responses

- scalar metric potentials;
- vector and frame-dragging responses;
- tensor responses;
- curvature and entropy responses;
- Weyl potential;
- metric slip;
- expansion and volume perturbations where defined;
- lensing-source responses;
- integrated metric-source histories.

## 9.3 Radiative responses

```text
Delta^X_{ell A}(k)
```

for:

- temperature intensity;
- E polarization;
- B polarization;
- Doppler sources;
- last-scattering sources;
- integrated sources;
- scalar, vector, and tensor projections;
- recombination-radiation and intrinsic distortion sources where linearly active.

## 9.4 Growth responses

- species-resolved density growth;
- velocity and momentum growth;
- total matter growth;
- compression-relic growth;
- dissipative-tail growth where active;
- neutrino and free-streaming response;
- scale-dependent growth and growth-rate response;
- relative-sector and entropy-mode response.

---

# 10. Direct hierarchy and line-of-sight equivalence

H[I] must preserve two independent response routes:

1. direct integration of the full frozen hierarchy;
2. line-of-sight construction from frozen source functions.

For every mandatory response channel, compare:

- amplitude;
- phase;
- peak and zero locations;
- asymptotic behavior;
- gauge-invariant observables;
- integrated source contributions;
- uncertainty.

Disagreement beyond the declared channel-specific tolerance is a response obstruction. It may not be hidden inside an aggregate score.

---

# 11. Transfer composition and restart identity

For lawful segmented evolution, H[I] must satisfy:

```text
T(eta_3,eta_1)
=
T(eta_3,eta_2) T(eta_2,eta_1)
```

within declared physical and numerical uncertainty.

A restart at any lawful checkpoint must reproduce:

- state vectors;
- phase and normalization;
- constraint residuals;
- hierarchy state;
- approximation state;
- source functions;
- branch identity;
- response ancestry.

Equal final scalar values do not establish equivalent histories.

---

# 12. Gauge and representation closure

H[I] must establish:

- gauge transformation consistency;
- agreement of gauge-invariant observables;
- correct removal of pure-gauge modes;
- superhorizon cancellation where required;
- consistency of direct and projected variables;
- consistency of species, metric, and finite-field representations;
- valid transformations at event and approximation switches.

A gauge-dependent response may be exported only with its gauge declaration and transformation map.

---

# 13. Conservation and geometric constraints

At every represented scale and time, H[I] must evaluate:

- Hamiltonian or corresponding geometric constraint;
- momentum constraint;
- total energy-transfer closure;
- total momentum-transfer closure;
- species continuity and momentum closure;
- charge and number closure where relevant;
- collision balance;
- compensation conditions;
- hierarchy-truncation residual;
- stiff-switch continuity;
- background-instantiation consistency.

Residuals must remain scale- and time-resolved.

A small integrated residual may not hide a failed local, species, mode, or event condition.

---

# 14. Uncertainty and covariance propagation

The H[I] response covariance must distinguish:

```text
Sigma_H[I] =
  J_H Sigma_Hᵁ J_H^T
  + J_I Sigma_I J_I^T
  + Sigma_discretization
  + Sigma_truncation
  + Sigma_interpolation
  + Sigma_switch
  + Sigma_solver
  + Sigma_branch
```

The export must include:

- response covariance by mode, species, sector, scale, and time;
- background-response cross-covariance;
- response Jacobians;
- interpolation and derivative uncertainty;
- direct/line-of-sight discrepancy covariance;
- branch covariance or separate branch packets;
- model-form uncertainty where an interface law remains bounded rather than derived.

H[I] does not combine this uncertainty with primordial-amplitude covariance. That belongs to Module J.

---

# 15. Branches and obstruction

For every admitted Module I branch, H[I] may produce:

- one unique response operator;
- a representation- or gauge-equivalent family;
- multiple physically distinct response branches;
- obstruction.

Branches remain distinct when they differ in:

- geometry;
- sector evolution;
- event history;
- dark-sector constitutive behavior;
- topology or anisotropy;
- interaction history;
- response phase or growth;
- pathology status.

Similar projected spectra are not sufficient to establish physical equivalence.

No branch may be selected because it appears closer to public observations.

---

# 16. H[I] output to Module J

H[I] must export:

```text
P_H[I]->J = (
  FrozenOperatorIdentity_H,
  BackgroundIdentity_I,
  InstantiationIdentity_H[I],
  DomainCompliance_I,
  B_H[I],
  ModeBasis_H,
  ModeNormalization_H,
  Gauge_H,
  GaugeInvariantMap_H,
  SpeciesSectorRegistry_H,
  DirectHierarchyResponses_H[I],
  LOSResponses_H[I],
  Delta_X_lA(k),
  T_aA(k,eta),
  MetricResponses_H[I],
  WeylAndLensingResponses_H[I],
  GrowthResponses_H[I],
  CollisionAndSwitchHistories_H[I],
  SourceHistories_H[I],
  GreenFunctions_H[I],
  TransferComposition_H[I],
  ResponseJacobians_H[I],
  Sigma_H[I],
  ConstraintResiduals_H[I],
  ConservationResiduals_H[I],
  GaugeResiduals_H[I],
  NumericalErrorMaps_H[I],
  BranchAndPathologyState_H[I],
  IntrinsicTransferSignatures_H[I],
  Memory_H[I],
  Ancestry_H[I],
  Restart_H[I]
)
```

Alongside this packet, Module J receives the immutable Module I background packet.

Module J must not reconstruct either parent.

---

# 17. Quantities explicitly reserved for Module J

H[I] does not assign:

- primordial mode amplitudes;
- primordial covariance;
- mode cross-correlation amplitudes;
- phase probability laws;
- statistical homogeneity or isotropy unless already derived upstream;
- Gaussian or non-Gaussian ensemble status;
- scalar or tensor normalization;
- spectral tilt, running, features, or cutoffs;
- realized CMB spectra;
- realized matter or sector spectra;
- growth normalization;
- finite-volume random fields;
- cosmic variance;
- the nonlinear promotion surface.

H[I] supplies unit responses. Module J supplies the internally derived primordial statistics that populate them.

---

# 18. Concatenated I→H[I] theorem target

The Module I and H[I] plans concatenate coherently when the following statement can be earned:

> Module I supplies a branch-specific realized RFC background whose geometry, clocks, expansion, sector histories, dark-sector laws, transfers, events, horizons, distances, derivatives, covariance, memory, ancestry, and numerical representation lie inside the frozen Module Hᵁ admissible domain. H[I] inserts that background through the unique declared map, preserves the frozen perturbation state, modes, gauges, collisions, hierarchies, closures, source grammar, uncertainty definitions, and scientific criteria, and generates complete basis-normalized species, metric, radiative, polarization, growth, Green-function, and line-of-sight responses with scale- and time-resolved conservation, gauge, uncertainty, branch, and restart closure. No public background, transfer function, spectrum, primordial normalization, or observational target is used, and primordial covariance remains entirely reserved for Module J.

This boundary prevents the realized background from becoming a hidden opportunity to redesign the transfer operator or fit the later linear universe.