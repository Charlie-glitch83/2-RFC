# Module O — Detailed Scientific Repair Plan

## Immutable Universe Identity, Restoration, Reproducibility, and Branch Isolation

## 1. Basis and scope

This plan is built from:

1. `architecture/2RFC_DETAILED_SCIENTIFIC_GAP_TO_LIBRARY_REPAIR_PLAN.md` as the primary scientific repair spine;
2. `modules/N/MODULE_N_DETAILED_SCIENTIFIC_REPAIR_PLAN.md` as the complete manifested-universe parent;
3. `modules/N/MODULE_N_TO_O_SCIENTIFIC_HANDOFF.md` as the sealed N→O physical-freeze boundary;
4. `Module_O_Individual_Enhancement_Plan.md` as the exact individualized statement of Module O science;
5. `Module_P_Individual_Enhancement_Plan.md` as the read-only empirical-child boundary;
6. `Module_Q_Individual_Enhancement_Plan.md` as the isolated physical-continuation boundary;
7. applicable Presentation 29 and Presentation 30 freeze, identity, branch, memory, replay, observer, and cycle-continuity science;
8. Module A route, event, branch, memory, promotion, and no-loss grammar at its exact scientific scope.

The old Module O run sequence, source-register administration, proof-obligation ledger, gate taxonomy, certificate architecture, repository lifecycle, archive namespace, branch bureaucracy, and evidence paperwork are not adopted as scientific content.

The module is the unit of scientific repair.

Module O begins from:

```text
one complete restartable Module N manifested universe
```

It ends with:

```text
one immutable parent universe identity
+
one read-only empirical child authorization for P
+
one isolated physical-continuation child authorization for Q
```

The protected dependency is:

```text
N -> O -> {P,Q}
P -/-> O
P -/-> Q
Q -/-> O
```

---

# 2. Canonical scientific responsibility

Module O must preserve one completed Module N universe as an immutable scientific object whose physical state, law stack, branch and realization identity, uncertainty, memory, lineage, truth observables, and exact continuation state can be restored and scientifically reproduced without altering the parent.

Its central theorem target is:

> Given one complete Module N manifested universe and the immutable A–N physical-law and lineage chain, RFC assigns one canonical scientific identity to that universe, freezes its physical and numerical continuation state, preserves uncertainty and no-loss ancestry, establishes exact restoration, restart continuity, and declared scientific-equivalence classes, and authorizes two isolated descendants: a read-only empirical-comparison child and a continued-physics child. Neither descendant may mutate the parent, and no empirical object may enter the continued-physics branch.

The direct claim is:

```text
RFC preserves one manifested universe as an immutable,
restartable, scientifically reproducible parent and derives
two isolated descendants for empirical testing and future physics.
```

Module O is not:

- a physical-generation module;
- a better-universe selector;
- an empirical fitting stage;
- a repair stage after public comparison;
- a branch-pruning stage based on observational success;
- a claim-upgrading stage;
- a terminal-cosmology module;
- a substitute for Module N physical completeness.

Its proof object is the immutable identity and lawful child relation of one already completed universe.

---

# 3. Exact parent state

Module O consumes:

```text
P_N->O = (
  UniverseAssemblyIdentity_N,
  CycleBranchRealization_N,
  ManifestedEpoch_N,
  GeometryCausality_N,
  ClockCoordinateGauge_N,
  EntityIdentityRegistry_N,
  OwnershipRegistry_N,
  WorldlineWorldtubeRegistry_N,
  GlobalEventGraph_N,
  GlobalCausalGraph_N,
  GlobalLineageGraph_N,
  MatterSectorState_N,
  CosmicStructureState_N,
  GasPlasmaMolecularState_N,
  StellarPopulationState_N,
  CompactObjectState_N,
  ChemicalIsotopeState_N,
  DustMoleculeState_N,
  RadiationBackgroundState_N,
  ReionizationLateThermalState_N,
  MagneticCosmicRayState_N,
  LensingTruthState_N,
  GravityWaveTruthState_N,
  TransientRegistry_N,
  TruthLightconeState_N,
  LocalEnvironmentRegistry_N,
  PhysicalRecordRegistry_N,
  MeasurementEventRegistry_N,
  ObserverReadyRegistry_N,
  BranchIdentityRegistry_N,
  PromotionRefinementRegistry_N,
  ConservationConstraint_N,
  Sigma_N,
  PhysicalMemory_N,
  RecursiveMemory_N,
  IntrinsicSignature_N,
  TruthObservableRegistry_N,
  ScopeUnresolvedState_N,
  Restart_N
)
```

No omitted material field may be reconstructed from a conventional cosmology package, public catalogue, inferred best fit, or undocumented default.

---

# 4. Where the current repository misses the mark

Module O is scientifically absent from the current 2-RFC branch.

The repository therefore lacks:

- one immutable universe identity;
- a physically defined freeze surface;
- a complete restart state;
- frozen law, constant, coordinate, gauge, and validity-domain identity;
- branch and realization identity;
- random and stochastic state preservation;
- uncertainty and covariance freeze;
- no-loss lineage and memory preservation;
- restoration and restart-continuity requirements;
- declared scientific-equivalence classes;
- an isolated read-only Module P child;
- an uncontaminated Module Q continuation child;
- P→Q firewall closure;
- lawful supersession without mutation of the prior parent.

A saved simulation directory or checksum list cannot substitute for these missing scientific relations.

---

# 5. Triadic realization

Module O must show the triad as operational identity and branch physics rather than administrative labels.

## 5.1 CIF_O

`CIF_O` is the admitted preservation and descendant space:

- candidate freeze surfaces;
- candidate representation-preserving restorations;
- candidate equivalent reproductions;
- candidate lawful migrations;
- P and Q descendant views;
- lawful superseding parent versions;
- rejected mutations and contaminated branches.

## 5.2 QV_O

`QV_O` is the qualification action that:

- tests parent completeness;
- fixes the physical freeze surface;
- verifies restoration and restart continuity;
- qualifies scientific equivalence;
- detects state, law, branch, lineage, uncertainty, or memory mutation;
- isolates P and Q;
- rejects public-data contamination;
- preserves old parent identity under later revisions.

## 5.3 RFL_O

`RFL_O` is the stabilized frozen universe identity:

```text
one immutable parent
+ one read-only P descendant
+ one uncontaminated Q descendant
+ restoration
+ scientific equivalence
+ uncertainty
+ memory
+ ancestry
```

The module action is:

```text
CIF_O -> QV_O -> RFL_O
```

---

# 6. Canonical Module O state

The Module O scientific state is:

```text
U_O = (
  ParentIdentity_O,
  FrozenPhysicalState_O,
  FrozenLawState_O,
  FrozenRealizationState_O,
  FrozenUncertainty_O,
  FrozenLineageMemory_O,
  RestorationState_O,
  ReproductionState_O,
  TruthInterface_O,
  PAuthorization_O,
  QAuthorization_O,
  IsolationState_O
)
```

Each component must declare:

- scientific meaning;
- source and owner;
- physical or numerical status;
- identity relation;
- allowed transformations;
- forbidden mutations;
- uncertainty;
- branch relation;
- restoration role;
- downstream visibility.

---

# 7. Physical freeze surface

Module O must freeze the universe on a physically meaningful surface.

Possible forms include:

- constant physical time;
- constant conformal time;
- constant scale factor or redshift;
- covariantly defined hypersurface;
- synchronized local surfaces when a global slice is not physically sufficient.

The freeze definition must preserve:

- geometry and causal meaning;
- coordinate and gauge status;
- event chronology;
- particle, field, fluid, radiation, chemical, and object synchronization;
- delayed processes and pending events;
- worldline and worldtube continuity;
- branch identity;
- restart sufficiency.

A wall-clock timestamp or file-creation time is not a physical freeze surface.

---

# 8. Immutable scientific identity

Module O assigns one parent identity:

```text
ID_O = I(
  cycle,
  physical branch,
  realization,
  freeze surface,
  physical state,
  law state,
  numerical continuation state,
  uncertainty,
  lineage,
  memory,
  truth interface
)
```

The identity must change when any claim-bearing element changes, including:

- physical state;
- law or constant;
- branch or realization;
- freeze surface;
- unresolved scope;
- uncertainty or covariance;
- truth projection;
- restart state;
- lineage or memory relation.

Storage location, filename, process ID, or archive copy does not define scientific identity.

A new physical version creates a new parent. It does not overwrite the old parent.

---

# 9. Complete continuation state

The Q-facing continuation state must preserve all variables needed for lawful forward evolution, including where active:

- metric, connection, curvature, and constraint variables;
- particle and phase-space state;
- field amplitudes and conjugate variables;
- fluid conserved and primitive variables;
- radiation and neutrino state;
- chemical, isotope, dust, and molecular state;
- stellar, compact-object, accretion, and promoted-object internal state;
- delayed processes and event queues;
- adaptive refinement and representation state;
- random and stochastic state;
- physical, conformal, proper, event, and integration clocks;
- solver history that is physically required for restart.

A derived field may be omitted only if its lawful reconstruction is explicit and scientifically equivalent.

---

# 10. Frozen law state

Module O must preserve:

- triad and First Action;
- recursive kernel and relational carrier;
- Big-Implosion and cycle law;
- all module-specific equations and couplings;
- all generated constants;
- transparent interface laws and their status;
- units, dimensions, coordinates, gauges, and conventions;
- validity domains;
- switching and embedding rules;
- approximation and closure domains;
- intrinsic falsifiers and unresolved boundaries.

Freezing an interface law does not convert it into an internally derived RFC law.

Freezing a benchmark does not authorize it as a generative input.

---

# 11. Randomness, ensembles, and branches

Module O must preserve:

- random-generator law and state;
- seed hierarchy;
- stream and substream assignment;
- phase realization;
- ensemble identity and weights;
- stochastic process state;
- physical branch identity;
- numerical branch identity;
- unresolved branch families;
- cross-realization and cross-branch covariance.

The following must remain distinct:

```text
same parent, exact restart
same laws, different realization
same realization, different physical branch
different numerical path, equivalent physical state
different model or law state
```

A changed realization is not a silent rerun of the same parent.

---

# 12. Uncertainty and covariance preservation

Module O freezes the full uncertainty state:

```text
Sigma_O = Freeze(Sigma_N)
```

It must retain material correlations among:

- geometry and matter;
- ordinary and dark sectors;
- structure and lensing;
- gas, stars, remnants, and chemistry;
- radiation, sources, and reionization;
- events, worldlines, and transients;
- environments, records, and observer-ready systems;
- truth observables;
- representations, resolutions, branches, and realizations.

Unknown uncertainty remains unknown. It may not be set to zero to simplify freezing or comparison.

Module P propagates the frozen uncertainty through observation models.

Module Q propagates it through future physics.

---

# 13. Restoration

Module O must establish that the complete parent can be restored without:

- hidden defaults;
- mutable external state;
- missing objects;
- manual inference;
- public-data reconstruction;
- cached-output substitution;
- undocumented branch selection.

Restoration must recover:

- physical state;
- physical clocks;
- law state;
- branch and realization state;
- uncertainty and covariance;
- event and causal state;
- lineage and memory;
- truth interface;
- exact continuation boundary.

A partially recovered universe is a different scientific object unless the omitted content is formally irrelevant to all claims and descendants.

---

# 14. Restart continuity

Let `U_O(t_f)` be the frozen parent and `Phi_Delta` the frozen forward evolution over a short declared interval.

The restored parent must satisfy:

```text
Phi_Delta(Restore(U_O(t_f)))
~= U_N(t_f + Delta)
```

within the declared equivalence and uncertainty domain.

The overlap must cover load-bearing:

- fields;
- objects;
- events;
- constraints;
- conservation;
- stochastic state;
- branch identity;
- covariance;
- memory and ancestry.

Restart continuity does not require a later Q descendant to remain identical after genuine future branching.

---

# 15. Scientific reproduction

Module O must distinguish:

## 15.1 Exact-state reproduction

Reconstruction of the same parent state.

## 15.2 Forward-overlap reproduction

Reproduction of a bounded continuation interval from the same parent.

## 15.3 Independent scientific reproduction

A materially independent implementation reproduces the load-bearing scientific state and conclusions within declared equivalence classes.

## 15.4 Ensemble reproduction

A frozen ensemble definition reproduces distributions, covariance, branch weights, and protected higher-order structure.

A scalar score or one favorable observable cannot establish reproduction of a complete universe.

---

# 16. Equivalence classes

Module O must declare which equivalence is required for every protected component:

```text
bitwise
bounded numerical
statistical
gauge or coordinate
representation
physical-state
scientific-claim
```

Equivalence must remain resolved across:

- fields and modes;
- objects and events;
- worldlines and lineages;
- conservation and constraints;
- uncertainty and covariance;
- records and truth observables;
- memory and branches.

A coordinate transformation may preserve physics while changing representation.

A matching scalar may hide a physically inequivalent state.

---

# 17. Lineage and no-loss preservation

Every frozen physical object must remain traceable through:

```text
triad
-> First Action
-> kernel and relational law
-> Big Implosion
-> Modules C-N
-> final entity, event, record, or truth observable
```

Module O must preserve:

- source and law ancestry;
- route and branch ancestry;
- event sequence;
- scale promotions;
- object descendants;
- physical memory;
- recursive memory;
- truth-observable propagation;
- uncertainty and covariance ancestry.

No promoted object may lose information required by Module P observation construction or Module Q terminal dissolution and memory qualification.

---

# 18. Memory separation

Module O preserves but does not conflate:

```text
M_rec         recursive memory
M_dyn         retained dynamical physical state
M_record      manifested physical records
M_checkpoint  numerical continuation state
M_archive     preservation state
```

Only the first three are physical states of the universe.

`M_checkpoint` permits exact continuation.

`M_archive` preserves scientific identity.

Neither becomes physical memory merely by storing information.

---

# 19. Intrinsic signatures and falsifiers

Before any Module P public comparison, Module O freezes:

- intrinsic RFC signatures;
- physical carriers;
- scale and time domains;
- branch and realization status;
- uncertainty and covariance;
- degeneracies;
- falsifiers;
- claim scope;
- unresolved boundaries.

Module P may test these objects.

Module P may not redefine them after seeing residuals.

Module Q may carry their physical ancestors forward, but may not receive P comparison outcomes.

---

# 20. Parent immutability

The parent is:

```text
U_O^parent = Freeze(P_N->O)
```

The freeze adds scientific identity and preservation but no new physical content.

Forbidden parent mutations include:

- changing physical fields;
- changing laws or constants;
- changing branch or realization;
- changing uncertainty;
- changing truth observables;
- changing unresolved scope;
- changing lineage or memory;
- replacing a failed reproduction with a repaired state under the same identity.

A lawful correction creates a new parent identity with explicit ancestry to the superseded one.

---

# 21. Module P read-only child

Module O exports:

```text
P_O->P = (
  ParentUniverseIdentity_O,
  PBranchIdentity,
  ReadOnlyTruthInterface_O,
  TruthObservableProjection_O,
  LineageQuery_O,
  IntrinsicSignatureAndFalsifier_O,
  FrozenUncertaintyCovariance_O,
  BranchEnsembleState_O,
  ScopeUnresolved_O,
  PWriteProhibition,
  PtoQProhibition
)
```

Module P may construct:

- signal and propagation projections not already represented at truth level;
- detector and instrument models;
- survey masks and selection;
- noise and foreground models;
- catalogues and maps;
- statistics and likelihoods;
- public-data comparisons and claim decisions.

All P-created objects remain outside the parent and Q child.

---

# 22. Module Q physical-continuation child

Module O exports:

```text
P_O->Q = (
  ParentUniverseIdentity_O,
  QBranchIdentity,
  PhysicalRestartState_O,
  FrozenLawState_O,
  ActiveFutureSectorState_O,
  GeometryCausalityClock_O,
  ConstraintConservation_O,
  PhysicalRecursiveMemory_O,
  EventRouteBranchScaleLineage_O,
  FrozenUncertaintyCovariance_O,
  PromotionReopening_O,
  TerminalFacingState_O,
  QPublicInputProhibition,
  QParentWriteProhibition
)
```

Module Q may:

- continue physical chronology;
- evolve all active future sectors;
- generate new descendant events and branches;
- derive terminal dissolution;
- derive the Big Rip as the required canonical terminal event;
- qualify latent memory;
- condition the next effective CIF.

Module Q may not alter the frozen parent or receive any P-created information.

---

# 23. Sibling isolation

The required dependency conditions are:

```text
Deps(Q) intersect Artifacts(P) = empty
```

```text
Writes(P,U_O^parent) = empty
Writes(Q,U_O^parent) = empty
```

The children may share only immutable parent objects authorized by Module O.

Forbidden P→Q inputs include:

- public observations;
- residuals;
- likelihoods;
- posteriors;
- fitted nuisance values;
- empirical branch preferences;
- validation decisions;
- public-data-inspired repairs;
- failed or passed score information.

A detected path blocks the Q descendant from supporting a clean RFC terminal claim.

---

# 24. Physical revision and supersession

When a physical change is required after freezing:

```text
U_O^parent(old)
-> revised A-N descendant
-> U_O^parent(new)
```

The old parent remains immutable.

The new parent must identify:

- changed physical content;
- changed law or interface;
- changed branch or realization;
- changed uncertainty or scope;
- reason for revision;
- ancestry relation to the prior parent;
- which prior claims no longer apply.

Module P residuals may motivate future scientific investigation, but they may not silently rewrite the tested parent or flow into the existing Q branch.

---

# 25. Persistence and migration

The parent scientific identity must survive:

- redundant copies;
- storage-media replacement;
- loss of one copy;
- lawful format migration;
- software and hardware obsolescence;
- partial retrieval;
- reconstruction in an independent environment.

A lawful migration preserves:

- semantics;
- physical state;
- numerical meaning;
- units and conventions;
- uncertainty and covariance;
- lineage and memory;
- branch identity;
- truth-observable meaning;
- restart sufficiency.

A migration that changes physical content creates a new parent.

---

# 26. Adversarial scientific tests

Module O must detect and reject:

- altered physical state;
- omitted protected object;
- severed lineage;
- hidden default;
- changed seed or stream;
- missing uncertainty;
- false zero covariance;
- mutable external dependency;
- stale cached output presented as recomputation;
- parent writes from P or Q;
- P-derived information entering Q;
- unauthorized law or constant replacement;
- failed restart overlap;
- representation equivalence that hides physical disagreement;
- migration that changes semantic or scientific meaning.

These are scientific identity and branch tests, not an administrative score system.

---

# 27. Exact Module O outputs

Module O exports three distinct packets.

## 27.1 Frozen parent

```text
P_O^parent = (
  UniverseIdentity_O,
  ParentManifestedState_O,
  FreezeSurface_O,
  FrozenLawState_O,
  FrozenNumericalRealization_O,
  FrozenRandomEnsembleBranch_O,
  FrozenUncertaintyCovariance_O,
  FrozenLineageMemory_O,
  FrozenTruthObservables_O,
  FrozenIntrinsicSignatures_O,
  FrozenScopeUnresolved_O,
  RestorationState_O,
  RestartContinuity_O,
  ReproductionEquivalence_O,
  ParentImmutability_O
)
```

## 27.2 P child

```text
P_O->P = (
  ParentUniverseIdentity_O,
  PBranchIdentity,
  ReadOnlyTruthInterface_O,
  TruthObservableProjection_O,
  LineageQuery_O,
  IntrinsicSignatureAndFalsifier_O,
  FrozenUncertaintyCovariance_O,
  BranchEnsembleState_O,
  ScopeUnresolved_O,
  PWriteProhibition,
  PtoQProhibition
)
```

## 27.3 Q child

```text
P_O->Q = (
  ParentUniverseIdentity_O,
  QBranchIdentity,
  PhysicalRestartState_O,
  FrozenLawState_O,
  ActiveFutureSectorState_O,
  GeometryCausalityClock_O,
  ConstraintConservation_O,
  PhysicalRecursiveMemory_O,
  EventRouteBranchScaleLineage_O,
  FrozenUncertaintyCovariance_O,
  PromotionReopening_O,
  TerminalFacingState_O,
  QPublicInputProhibition,
  QParentWriteProhibition
)
```

The parent, P child, and Q child are different scientific identities with explicit ancestry.

---

# 28. Reserved scope

## Reserved for Module P

- instruments and detector response;
- surveys and selection;
- foregrounds, noise, and calibration;
- observation-equivalent products;
- public datasets;
- likelihoods and residuals;
- empirical validation and falsification;
- claim decisions.

## Reserved for Module Q

- continued physical evolution;
- late accelerated dynamics;
- dissipative-tail evolution;
- route-by-route dissolution;
- canonical Big Rip;
- terminal branch closure;
- latent recursive-memory qualification;
- next-effective-CIF conditioning.

Module O owns neither empirical comparison nor terminal physics.

---

# 29. Logical scientific repair order

This is not a run sequence.

1. Admit one complete Module N parent.
2. Fix the physical freeze surface.
3. Freeze universe, cycle, branch, realization, and epoch identity.
4. Freeze the complete physical continuation state.
5. Freeze laws, constants, units, gauges, coordinates, validity domains, and interfaces.
6. Freeze random, stochastic, ensemble, and branch state.
7. Freeze uncertainty and covariance.
8. Freeze lineage, memory, truth observables, intrinsic signatures, and unresolved scope.
9. Establish exact restoration.
10. Establish restart continuity.
11. Establish component-specific scientific equivalence.
12. Establish independent scientific reproduction.
13. Verify no-loss promotion and reopening sufficiency.
14. Establish parent immutability.
15. Construct the read-only P child.
16. Construct the isolated Q child.
17. Prove sibling isolation.
18. Define lawful migration and supersession.
19. Apply adversarial identity and contamination tests.
20. Export the parent and both children.

---

# 30. Module O completion statement

The repaired Module O should support this direct statement:

> From one complete Module N manifested universe, RFC defines a physically meaningful freeze surface, preserves every claim-bearing field, object, event, branch, record, memory, truth observable, uncertainty, covariance, law, and restart variable, and assigns one immutable scientific identity. The parent can be restored and continued over a declared overlap interval, and its load-bearing scientific state can be reproduced under explicit component-specific equivalence classes. Module O then derives two isolated descendants: a read-only Module P empirical child and a Module Q physical-continuation child. Neither descendant can mutate the parent, and no Module P information can influence Module Q.

The module must simultaneously state what it does not yet prove:

- empirical agreement with public observations;
- predictive confirmation;
- continued terminal evolution;
- canonical Big-Rip execution;
- latent-memory qualification;
- next-cycle conditioning.

Those belong to Modules P and Q.