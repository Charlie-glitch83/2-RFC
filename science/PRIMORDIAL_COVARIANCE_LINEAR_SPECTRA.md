# Module J — Primordial Covariance, Linear Spectra, Growth, and Nonlinear-Ready Fields

## 1. Scientific boundary

Modules A through I, Module Hᵁ, and Module H[I] are complete and frozen at their declared scopes. Module J consumes two immutable direct parents:

\[
P_{J,\mathrm{in}}
=\left(P_{H[I]\to J},P_{I\to J}\right),
\]

where \(P_{H[I]\to J}\) contains the frozen basis-normalized linear responses on the realized Module I background and \(P_{I\to J}\) contains that background, geometry, clocks, sector histories, events, horizons, distances, derivatives, covariance, branch identity, memory, ancestry, and restart state.

Module J derives the realized linear statistical universe

\[
\mathcal U_J^{\mathrm{lin}}
=(\mathcal P_J,\mathcal T_J,\mathcal S_J,\mathcal G_J,
\mathcal F_J,\mathcal D_{\mathrm{lin}},\Sigma_J,M_J,
\mathrm{Anc}_J,\mathrm{Restart}_J),
\]

where:

- \(\mathcal P_J\) is the endogenous primordial mode covariance and declared higher-moment state;
- \(\mathcal T_J\) is the immutable H[I] response ancestry;
- \(\mathcal S_J\) is the complete linear spectrum and cross-spectrum registry;
- \(\mathcal G_J\) is the species-, sector-, scale-, and time-resolved growth state;
- \(\mathcal F_J\) is the gauge- and constraint-consistent finite-volume field ensemble;
- \(\mathcal D_{\mathrm{lin}}\) is the witnessed physical linearity domain and nonlinear-promotion surface.

The completed Module J theorem is:

> Given one admitted Module I/H[I] parent branch, the enhanced completed Module A triad kernel, the frozen Big-Implosion common ancestry, the complete independent H[I] unit-mode basis, and the realized Module I geometry, RFC constructs a branch-specific primordial covariance from generated route, shell, memory, symmetry, scale, and source carriers. At every declared finite refinement the covariance is Hermitian, positive semidefinite, dimensionally typed, finite on its admitted infrared and ultraviolet support, and compatible with the physical mode quotient, reality conditions, branch identities, and conservation constraints. Contracting it with the immutable H[I] responses generates the complete primary linear radiative, matter, metric, neutrino, compression-relic, dissipative-tail, field, parity, helicity, and cross-sector spectrum matrices and growth histories. A separately realized ensemble reproduces the transfer-covariance construction within declared finite-volume and sampling uncertainty; the resulting fields preserve phase, gauge, constraints, route identity, covariance, memory, ancestry, and restart state; and a componentwise physical error map defines the nonlinear-promotion surface exported to Module K without public-data fitting or upstream retuning.

The result is complete at a **finite-relational, internally normalized, linear-statistical scope**. It is not nonlinear gravitational evolution, a fitted primordial power law, an observed CMB reconstruction, a public transfer table, a survey likelihood, or empirical confirmation.

---

## 2. Immutable parent pair

### 2.1 H[I] response parent

The frozen response parent supplies:

- operator, background, instantiation, and domain-compliance identities;
- the complete independent initial-mode basis and normalization;
- scalar, vector, tensor, helicity, parity, entropy, compensated, relative-sector, field, defect, dark-sector, and RFC-specific mode labels where active;
- direct-hierarchy, line-of-sight, Green-function, species, metric, photon, polarization, growth, and projected-source responses;
- gauge transformations and gauge-invariant projections;
- geometric, species, collision, continuity, momentum, and compensation constraints;
- response derivatives, transfer covariance, approximation, interpolation, and numerical-error maps;
- branch, pathology, signature, memory, ancestry, and restart state.

The H[I] responses may be evaluated and contracted. Their state dimension, equations, basis, phase convention, gauge system, collisions, hierarchy, source grammar, closures, and scientific tolerances may not be modified.

### 2.2 Module I background parent

The realized background parent supplies:

- geometry, topology, slicing, connection, curvature, averaging, and backreaction state;
- physical time, conformal time, scale, redshift, affine, and observer maps;
- ordinary, radiative, relic, compression-relic, dissipative-tail, field, defect, magnetic, and other admitted sector histories;
- witnessed inter-sector transfer currents;
- expansion, acceleration, event, horizon, distance, ruler, clock, siren, and lensing-background geometry;
- internal dimensional units and reference events;
- background derivatives, covariance, branch, stability, causality, pathology, signature, memory, ancestry, and restart state.

Module J may not reconstruct or recalibrate this background.

---

## 3. Enhanced completed Module A kernel

Every Module J object specializes

\[
\widehat{\mathcal K}_A(\tau)
=\sum_{j=1}^{n_A}\sum_{[r]\in\mathcal A_A(\tau)}
\delta^{-j}e^{-\alpha j\tau}
W_{j,[r]}(\tau)\Phi_{j,[r]}(\tau),
\]

including:

- the ordered primitive triad \((CIF,QV,RFL)\);
- the First Action \(QV(CIF)\to RFL\);
- recursive depth, modal structure, bounded tails, and shell promotion;
- directed relations, routes, local witnesses, inverse/conjugate routes, and legal incidence;
- no-loss quotienting, protected identity, branch, obstruction, promotion, refinement, and reopening;
- uncertainty, covariance, memory, and complete ancestry;
- terminal N-body relational and solution-generating completion.

The terminal N-body carrier is active in multi-mode covariance, cross-sector ancestry, field realization, phase-space sampling, mode/route refinement, and nonlinear-promotion preparation. Where no Module J witness exists, it remains dormant, information-bearing, unchanged, and nonbackreacting.

---

## 4. Module J triadic specialization

Define

\[
CIF_J
=\Phi_J(P_{H[I]\to J},P_{I\to J},\widehat{\mathcal K}_A,M_I),
\]

\[
QV_J
=\Psi_J(CIF_J,P_{H[I]\to J},P_{I\to J},\widehat{\mathcal K}_A),
\]

\[
RFL_J
=\Omega_J(QV_J,P_{H[I]\to J},P_{I\to J},\widehat{\mathcal K}_A),
\]

\[
H_J
=\Gamma_J(CIF_J,QV_J,RFL_J,P_{H[I]\to J},P_{I\to J},\widehat{\mathcal K}_A).
\]

The roles are functional:

- **CIF\(_J\)** opens the lawful mode, route, correlation, phase, parity, helicity, branch, finite-volume, ensemble, and nonlinear-promotion possibilities already admitted by the parents;
- **QV\(_J\)** qualifies covariance support, enforces Hermiticity, positivity, dimensions, reality, regularity, gauge, conservation, finite power, branch conditions, response contraction, field generation, uncertainty propagation, and linearity witnesses;
- **RFL\(_J\)** stabilizes the accepted covariance, spectra, growth, fields, phase/seed identity, promotion surface, signatures, memory, ancestry, restart state, and complete Module K parent.

Randomness enters only through declared realization variables after the physical covariance and higher-moment law are fixed.

---

## 5. Complete primordial mode basis

Let

\[
E_H(k)=\{e_A(k)\}_{A=1}^{N_M}
\]

be the frozen H[I] physical unit-mode basis after gauge quotient and constraint closure. Module J inherits it without deletion or renormalization.

Each mode carries:

- unique identifier and physical interpretation;
- scalar, vector, or tensor type;
- helicity and parity where active;
- species and sector support;
- gauge representation and gauge-invariant content;
- regularity, superhorizon, and branch domain;
- unit normalization and phase convention;
- source, route, shell, event, scale, branch, and recursive-memory ancestry;
- allowed correlation partners;
- independence or a proved no-loss quotient-equivalence witness.

The basis may contain total-curvature-like, adiabatic, entropy, compensated, relative-density, relative-velocity, baryon-photon, neutrino, compression-relic, dissipative-tail, vector, vortical, tensor, helicity, magnetic, defect, and RFC route-memory modes where generated.

A small expected amplitude is not an equivalence theorem and cannot remove a mode.

---

## 6. Random variables and statistical status

For each admitted mode define the primordial amplitude

\[
q_A(\kappa),
\]

where \(\kappa\) is the generated harmonic, relational-eigenmode, Fourier-like, or inhomogeneous mode label appropriate to the Module I geometry.

Module J distinguishes:

- deterministic parent and branch variables;
- stochastic amplitudes;
- stochastic phases;
- route multiplicity;
- ensemble variables;
- seed identity;
- finite-volume sampling noise;
- numerical noise.

The mean is

\[
\mu_A(\kappa)=\mathbb E[q_A(\kappa)].
\]

The centered covariance is

\[
\boxed{
\mathcal P_{AB}(\kappa,\kappa')
=\mathbb E[(q_A(\kappa)-\mu_A(\kappa))
(q_B(\kappa')-\mu_B(\kappa'))^*].
}
\]

The statistical constitution records support, phase law, reality, parity, helicity, homogeneity, isotropy, Gaussian/non-Gaussian status, higher cumulants, branch dependence, uncertainty, and mode-coupling restrictions.

Homogeneity, isotropy, Gaussianity, parity, and scale invariance are derived, bounded, approximate, broken, or unresolved. None is silently presumed.

---

## 7. Endogenous covariance generator

Let \(\mathcal R_J(\kappa)\) be the mode-by-source incidence matrix. Its columns are generated primordial source carriers, including where active:

- Big-Implosion common-ancestry carriers;
- recursive-shell and route-memory carriers;
- ordinary/radiative/compression/tail relative carriers;
- finite geometry, field, defect, helicity, and parity carriers;
- branch-specific event and promotion carriers.

Every entry is a projected kernel amplitude

\[
(\mathcal R_J)_{A\rho}(\kappa)
=\sum_j\delta^{-j}e^{-\alpha j\tau_\star}
W_{j,A\rho}(\kappa)
\langle e_A(\kappa)|\Phi_{j,A\rho}|s_\rho\rangle,
\]

with source \(s_\rho\), internal reference surface \(\tau_\star\), route, branch, units, and ancestry attached.

Let the generated source covariance be

\[
\Xi_J(\kappa,\kappa')
=\mathcal L_J(\kappa)\mathcal L_J(\kappa')^\dagger,
\]

where \(\mathcal L_J\) is constructed from nonnegative source occupation, branch, shell, legal-incidence, recursive-memory, and uncertainty carriers. Then

\[
\boxed{
\mathcal P_J(\kappa,\kappa')
=\mathcal R_J(\kappa)
\Xi_J(\kappa,\kappa')
\mathcal R_J(\kappa')^\dagger.
}
\]

This is deterministic conditional on the frozen parents and admitted branch. Realization randomness is not used to choose \(\mathcal P_J\).

The internally generated scale dependence enters through:

- relational Laplacian and geometry eigenvalues;
- route and shell amplitudes;
- finite source correlation lengths and event widths;
- recursive-depth damping and memory;
- H[I] unit-mode normalization;
- Module I dimensional units and clocks.

No amplitude, tilt, running, feature, cutoff, tensor ratio, or cross-correlation is imported.

---

## 8. Spectral representation and finite power

When the realized background admits a self-adjoint mode operator \(\mathcal L_{\mathrm{geom}}\), let

\[
\mathcal L_{\mathrm{geom}}\psi_n=\lambda_n\psi_n.
\]

The source kernel can be expanded as

\[
\Xi_J(\kappa,\kappa')
=\sum_{n=1}^{N_R}\sigma_n
u_n(\kappa)\nu_n(\kappa')^\dagger,
\qquad \sigma_n\ge0,
\]

with finite declared support or converged tail bound.

The admitted power condition is

\[
\boxed{
\int_{\mathcal K_J}
\operatorname{Tr}
\left[W_{\mathrm{phys}}(\kappa)
\mathcal P_J(\kappa,\kappa)\right]d\mu_J(\kappa)<\infty.
}
\]

Infrared and ultraviolet support, measure, dimensions, branch, truncation, uncertainty, and reopening rules are explicit. Divergent or negative-power branches are obstructed; they are not clipped into acceptance.

---

## 9. Hermiticity and positive semidefiniteness

The covariance obeys

\[
\mathcal P_J(\kappa,\kappa')
=\mathcal P_J(\kappa',\kappa)^\dagger.
\]

For any square-integrable mode test vector \(v_A(\kappa)\),

\[
\int d\mu\,d\mu'\,
 v(\kappa)^\dagger
\mathcal P_J(\kappa,\kappa')v(\kappa')
=\left\|\int d\mu\,
\mathcal L_J(\kappa)^\dagger
\mathcal R_J(\kappa)^\dagger v(\kappa)\right\|^2
\ge0.
\]

Thus the generated covariance is positive semidefinite by construction.

Zero eigenvalues are lawful only when they correspond to absent source support, exact compensation, a physical constraint, or proved no-loss equivalence. Negative numerical eigenvalues are diagnosed against precision and factorization residuals; they cannot be physically repaired by unexplained clipping.

---

## 10. Physical quotient, constraints, and covariance support

Let \(P_{\mathrm{phys}}\) be the frozen H[I] gauge/constraint physical projector. Module J requires

\[
\boxed{
\mathcal P_J
=P_{\mathrm{phys}}\mathcal P_JP_{\mathrm{phys}}^\dagger.
}
\]

For each linear constraint operator \(\mathbb C_H\),

\[
\mathbb C_H\mathcal P_J=0,
\qquad
\mathcal P_J\mathbb C_H^\dagger=0.
\]

This guarantees that ensemble support lies on the same physical constrained mode space as the frozen H[I] basis.

Pure-gauge variance is exactly zero. A gauge-dependent spectrum may still be exported only with its gauge declaration and transformation map.

---

## 11. Generated symmetry projection

Let \(G_J\) be the actually generated finite or compact automorphism/symmetry group of the admitted branch, acting through \(U(g)\) on mode space. If invariance is physically established, define

\[
\boxed{
\mathcal P_J^{G}
=\int_{G_J}U(g)\mathcal P_J^{\mathrm{raw}}U(g)^\dagger\,d\mu_G(g).
}
\]

This projection preserves Hermiticity and positive semidefiniteness.

If translation-like invariance is generated, then

\[
\mathcal P_{AB}(k,k')
=(2\pi)^{d_I}\delta_D(k-k')P_{AB}(k)
\]

under the branch's transform convention. If not, the full two-mode kernel is retained.

If rotational isotropy is generated, covariance depends only on invariant spectral data. If not, angular and directional dependence remains.

Parity and helicity are treated identically: vanishing odd or cross-helicity blocks require a derived symmetry, not convention.

---

## 12. Scalar, vector, tensor, parity, and helicity blocks

With frozen projectors \(P_S,P_V,P_T\), define

\[
P_{XY}=P_X\mathcal P_JP_Y^\dagger,
\qquad X,Y\in\{S,V,T\}.
\]

Thus

\[
\mathcal P_J=
\begin{pmatrix}
P_{SS}&P_{SV}&P_{ST}\\
P_{VS}&P_{VV}&P_{VT}\\
P_{TS}&P_{TV}&P_{TT}
\end{pmatrix}.
\]

Every block is classified as:

- required by source support;
- forbidden by a generated symmetry;
- exactly zero by route/constraint structure;
- nonzero through geometry, defect, magnetic, branch, route, or memory ancestry;
- boundedly negligible only within a named approximation.

Helicity projectors \(P_\pm\) and parity projectors \(P_{\mathrm e},P_{\mathrm o}\) generate left/right and even/odd covariance blocks. Tensor/vector power, chirality, TB/EB capacity, and scalar-vector/tensor cross-correlation remain active where sourced.

---

## 13. Adiabatic, entropy, compensated, and relative-sector covariance

Let \(Q_A\) be the frozen mode basis reordered into:

- total-curvature/adiabatic carrier;
- relative-density/entropy carriers;
- relative-velocity carriers;
- compensated carriers;
- baryon-photon and neutrino carriers;
- compression-relic/ordinary and tail/total carriers;
- RFC route-memory carriers;
- vector, tensor, field, and defect carriers.

The covariance block

\[
P_{AB}=\mathbb E[q_Aq_B^*]
\]

retains each auto- and cross-amplitude, phase, correlation coefficient, regularity witness, conservation role, branch, and ancestry.

For nonzero diagonal entries define

\[
r_{AB}(\kappa)
=\frac{P_{AB}(\kappa)}
{\sqrt{P_{AA}(\kappa)P_{BB}(\kappa)}},
\qquad |r_{AB}|\le1.
\]

Exact compensation is a constraint relation in the supported mode subspace, not cancellation imposed after spectra are generated.

---

## 14. Higher-order statistics boundary

Module J's mandatory theorem is the complete two-point state and linear evolution. The higher-order state is declared as

\[
\mathcal H_J^{(>2)}
=(\kappa_3,\kappa_4,\ldots),
\]

where \(\kappa_n\) are connected cumulant kernels.

Three cases are distinguished:

1. **Derived Gaussian closure:** every material connected cumulant above second order vanishes or is bounded below the declared Module K requirement;
2. **Derived non-Gaussian state:** material cumulants are generated, normalized, and exported with a lawful realization map;
3. **Unresolved higher moments:** the two-point Module J theorem remains valid, but Module K admission is restricted until the missing material statistics are derived or bounded.

Gaussianity is never assumed merely because a covariance factorization is convenient.

---

## 15. Immutable transfer-covariance contraction

Let \(T_a^A(\kappa,t)\) be the frozen H[I] response of species/sector/observable \(a\) to unit mode \(A\). Then

\[
\boxed{
P_{ab}(\kappa,\kappa';t,t')
=\sum_{A,B}
T_a^A(\kappa,t)
\mathcal P_{AB}(\kappa,\kappa')
T_b^B(\kappa',t')^*.
}
\]

In operator form,

\[
P_X=T_X\mathcal P_JT_X^\dagger.
\]

This preserves Hermiticity and positive semidefiniteness. It also preserves mode, route, phase, helicity, parity, species, sector, branch, memory, and dimensional ancestry.

The H[I] responses are not rescaled, refit, rephased, or structurally changed during contraction.

---

## 16. Primary linear radiative spectra

For projected radiative channels \(X,Y\) and generated homogeneous/isotropic measure where valid,

\[
\boxed{
C_\ell^{XY}
=\int d\mu_k\sum_{A,B}
\mathcal P_{AB}(k)
\Delta_{\ell A}^{X}(k)
\Delta_{\ell B}^{Y}(k)^*.
}
\]

The internal measure and normalization are inherited from Module I geometry and H[I] basis conventions. A familiar \(4\pi d\ln k\) representation is used only when that convention is derived.

The registry includes where active:

\[
TT,\ TE,\ EE,\ BB,\ TB,\ EB,
\]

with separate source contributions from:

- finite-width last scattering;
- Doppler/velocity sources;
- metric and integrated linear sources;
- scalar, vector, tensor, parity, and helicity modes;
- compression-relic and dissipative-tail perturbations;
- field, defect, spectral, route, and memory sources.

These are intrinsic pre-instrument primary linear spectra. Nonlinear lensing, late astrophysical reionization, foregrounds, beams, noise, masks, and likelihoods remain downstream.

---

## 17. Complete species, sector, and metric spectrum matrix

For projected state vector \(Y_a=T_aq\), define

\[
\mathbf P_J(\kappa,t)
=\mathbb E[Y(\kappa,t)Y(\kappa,t)^\dagger].
\]

The matrix includes, where active:

- total ordinary matter and baryons;
- compression relic;
- dissipative tail;
- photons, neutrinos, and other free-streaming species;
- density, velocity divergence, momentum, pressure, sound, and anisotropic stress;
- scalar/vector/tensor metric variables;
- Weyl potential, metric slip, lensing-source, and integrated-source variables;
- all material cross-spectra.

Every entry records gauge or gauge-invariant status, dimensions, normalization, scale/time domain, uncertainty, covariance, contribution decomposition, branch, route, memory, and ancestry.

A single total-matter spectrum is not a complete Module J output.

---

## 18. Linear growth and growth-rate histories

For a positive auto-spectrum entry and internal reference time \(t_\star\), define the covariance-normalized growth

\[
\boxed{
D_a(\kappa,t;t_\star)
=\sqrt{\frac{P_{aa}(\kappa,t)}{P_{aa}(\kappa,t_\star)}}.
}
\]

Where a signed unit-mode response is required, retain the transfer-defined growth \(T_a^A(t)/T_a^A(t_\star)\) separately.

The logarithmic rate is

\[
\boxed{
f_a(\kappa,t)
=\frac{d\ln D_a}{d\ln a_I}.
}
\]

Using \(\dot P=A P+P A^\dagger\),

\[
f_a
=\frac{1}{2H_I P_{aa}}
\frac{dP_{aa}}{dt}
\]

on a monotone scale branch.

The growth registry identifies scale-independent and scale-dependent regimes, radiation suppression, free-streaming suppression, compression-relic clustering transitions, dissipative-tail drag or enhancement, metric-slip effects, equality/acceleration events, growing/decaying eigenmodes, and memory-bearing transients.

Growth normalization comes only from \(\mathcal P_J\).

---

## 19. Acoustic, damping, and free-streaming ancestry

Module J preserves generated signatures of:

- sound horizon and acoustic phase;
- baryon loading and drag release;
- photon diffusion damping;
- neutrino/free-streaming phase and cutoff structure;
- compression-relic clustering/interaction scale;
- dissipative-tail transition scale;
- magnetic, defect, parity, helicity, and route-memory features.

Every feature is traced to a Module G source, Module I geometry/sector history, H[I] response, and Module J covariance carrier. No curve feature is introduced as a fitting degree of freedom.

---

## 20. Metric, Weyl, and projected source histories

Module J generates covariance and field histories for:

- gauge-invariant metric potentials;
- metric slip;
- potential growth and decay;
- Weyl potential;
- integrated metric-derivative sources;
- linear lensing-source kernels;
- redshift-space velocity kernels;
- number-density and volume-distortion source terms;
- ruler, clock, and siren perturbation kernels;
- gravitational-wave propagation responses where active.

These are linear truth/source objects. They are not nonlinear ray tracing, a survey observable, or a likelihood.

---

## 21. Gauge and conservation closure

Because the primordial covariance has support only on the physical H[I] mode quotient and H[I] propagates the frozen constraints,

\[
\mathbb C_H T_H\mathcal P_J=0
\]

within the declared operator and numerical tolerances.

Module J verifies, as functions of mode, scale, time, species, branch, and realization:

- gauge transformation consistency;
- agreement of gauge-invariant observables across frozen formulations;
- pure-gauge null support;
- superhorizon cancellation where required;
- total stress-energy conservation;
- species continuity and momentum equations;
- geometric constraints;
- compensation relations;
- covariance normalization and cross-spectrum symmetry;
- field-level reality and constraints;
- direct/transfer agreement.

No global average can override a failed local mandatory condition.

---

## 22. Direct ensemble evolution versus covariance contraction

Let \(L_J\) be any lawful factor satisfying

\[
\mathcal P_J=L_JL_J^\dagger.
\]

Let \(\xi^{(n)}\) be declared unit-covariance realization variables with the required reality, phase, branch, and higher-moment law. Define

\[
q^{(n)}=\mu+L_J\xi^{(n)}.
\]

Direct H[I] evolution gives

\[
Y^{(n)}(t)=T_H(t)q^{(n)}.
\]

Then

\[
\mathbb E[(Y-\bar Y)(Y-\bar Y)^\dagger]
=T_H\mathcal P_JT_H^\dagger.
\]

Module J implements both routes:

1. analytic covariance contraction;
2. direct evolution of a representative field ensemble.

They must agree for auto/cross spectra, mode decomposition, phase evolution, growth, projected spectra, and uncertainty within independently estimated sampling and numerical error.

---

## 23. Finite-volume realization generator

Choose a finite volume \(\mathcal V_J\), boundary condition, transform basis, mode support, mesh/particle representation, and internal start surface inside the Module J/Module K overlap domain.

For each independent positive mode \(k\), generate

\[
q(k)=\mu(k)+L_J(k)\xi(k),
\]

with reality pairing

\[
q(-k)=q(k)^*.
\]

The physical fields are

\[
\boxed{
X_a(x,t_{\mathrm{in}})
=\mathcal F^{-1}_{\mathcal V_J}
\left[\sum_A T_a^A(k,t_{\mathrm{in}})q_A(k)\right].
}
\]

The generator preserves:

- cross-mode and cross-species phase consistency;
- gauge and field-level constraints;
- branch, route, shell, parity, and helicity state;
- random seed, factorization, and phase identity;
- finite-volume geometry and boundary conditions;
- aliasing control and transform normalization;
- long-mode and missing-mode treatment;
- mesh/particle weights, stress, and distribution-facing state;
- covariance, uncertainty, memory, ancestry, and restart identity.

Paired/fixed amplitude or other variance-control ensembles are allowed only when the estimator remains unbiased for the declared statistic and the altered higher moments are explicitly recorded.

---

## 24. Finite-volume covariance recovery

For estimator

\[
\widehat P_{ab}(k)
=\frac{1}{N_k}\sum_{k_i\in\mathcal B_k}
X_a(k_i)X_b(k_i)^*,
\]

Module J derives its expectation, sampling covariance, finite-bin bias, finite-volume/missing-mode error, aliasing error, and branch dependence.

The generated ensemble must recover the target \(P_{ab}\) within

\[
\|\widehat P-P\|
\le
\varepsilon_{\mathrm{sampling}}
+\varepsilon_{\mathrm{finite\ volume}}
+\varepsilon_{\mathrm{bin}}
+\varepsilon_{\mathrm{alias}}
+\varepsilon_{\mathrm{numeric}}.
\]

A single realization may be passed downstream only with its ensemble identity, phase/seed record, covariance estimate, and conditions for representativeness.

---

## 25. Uncertainty and covariance constitution

Module J separates

\[
\boxed{
\Sigma_J
=J_{\mathrm{prim}}\Sigma_{\mathrm{prim}}J_{\mathrm{prim}}^\dagger
+J_H\Sigma_{H[I]}J_H^\dagger
+J_I\Sigma_IJ_I^\dagger
+\Sigma_{\mathrm{model}}
+\Sigma_{\mathrm{numeric}}
+\Sigma_{\mathrm{sampling}}
+\Sigma_{\mathrm{finite\ volume}}
+\Sigma_{\mathrm{cosmic}}
+\Sigma_{\mathrm{branch}}.
}
\]

The state includes covariance and cross-covariance by mode, species, sector, scale, time, observable, branch, and realization.

It records ensemble size, phase pairing, fixed-amplitude status, unbiased estimators, covariance-estimator uncertainty, seed reproducibility, and conditions for downstream use.

Cosmic variance does not replace model, transfer, background, finite-volume, or numerical uncertainty.

---

## 26. Identifiability and degeneracy

Let the observable response matrix be

\[
\mathcal M_O(k)=\{T_O^A(k)\}_{O,A}.
\]

Module J computes:

- covariance eigenvalues and eigenvectors;
- physical and source null spaces;
- transfer null spaces;
- near-degenerate mode combinations;
- scale-localized singular values;
- observable-space principal components;
- route-memory separability;
- sensitivity to background/transfer uncertainty;
- downstream channels required to distinguish retained modes.

A response-null primordial direction remains a physically retained mode unless no-loss equivalence is proved. It is exported with its identifiability status rather than erased.

---

## 27. Physical linearity error map

For each \((\kappa,t,s,r)\), define the componentwise witness vector

\[
\epsilon_{\mathrm{lin}}
=(\epsilon_{\mathrm{amp}},\epsilon_{\Delta^2},
\epsilon_{2},\epsilon_{\mathrm{disp}},
\epsilon_{\mathrm{shell}},\epsilon_{\mathrm{stress}},
\epsilon_{\mathrm{constraint}},\epsilon_{\mathrm{direct}},
\epsilon_{\mathrm{representation}}).
\]

Representative physical definitions include:

\[
\epsilon_{\mathrm{amp}}=\sqrt{P_{aa}},
\qquad
\epsilon_{\Delta^2}=\sqrt{\frac{d\operatorname{Var}(X_a)}{d\ln\kappa}},
\]

\[
\epsilon_2
=\frac{\|\mathcal N_2[X^{(1)},X^{(1)}]\|}
{\|\mathcal L_1X^{(1)}\|+\epsilon_0},
\]

\[
\epsilon_{\mathrm{disp}}
=\kappa^2\langle|\Psi_{\mathrm{Lag}}|^2\rangle,
\]

with additional shell-crossing, stress-closure, constraint, direct/transfer, and representation residuals.

The linearity domain is the conjunction

\[
\boxed{
\mathcal D_{\mathrm{lin}}
=\{(\kappa,t,s,r):
\epsilon_a(\kappa,t,s,r)
\le\epsilon_a^{\max}\ \text{for every mandatory }a\}.
}
\]

The nonlinear-promotion surface is the first physical crossing of any mandatory boundary with its event bracket, cause, uncertainty, branch, species, sector, route, and ancestry attached.

No average score can keep a point linear after one mandatory witness fails. No conventional single cutoff substitutes for this map.

---

## 28. Promotion overlap and Module K readiness

Let \(\mathcal D_{JK}\) be the domain in which the Module J linear description and the intended Module K nonlinear representation are both valid. Module J exports enough data to test

\[
S_J|_{\mathcal D_{JK}}
=S_K|_{\mathcal D_{JK}}
+\mathcal R_{JK},
\qquad
\|\mathcal R_{JK}\|\le\varepsilon_{JK}.
\]

The promotion state contains density, velocity, momentum, pressure, anisotropic stress, metric, radiation, neutrino, compression-relic, dissipative-tail, field, defect, gauge, constraint, covariance, phase, seed, branch, route, memory, and ancestry information.

Particle or mesh loading may not rescale amplitudes, replace phases, reassign species, or erase long modes.

---

## 29. Adaptive physical completeness

Let

\[
\mathfrak R_J^{(0)}
\hookrightarrow\mathfrak R_J^{(1)}
\hookrightarrow\cdots
\]

refine mode basis support, relational eigenmodes, route/shell carriers, \(k\)-domain, time sampling, multipoles, covariance rank, precision, ensemble size, volume, mesh, and linearity estimators.

A refinement is promoted when it:

- opens a previously unresolved lawful mode/route;
- closes a covariance, symmetry, conservation, phase, field, or ancestry gap;
- changes a claimed spectrum, growth, field, covariance, or promotion surface beyond the tail;
- resolves a pathology or degeneracy;
- is not a no-loss duplicate.

The module closes only when covariance eigenstructure, spectra, growth, direct/ensemble agreement, finite-volume recovery, uncertainty, and the nonlinear-promotion surface stabilize within declared bounds.

---

## 30. Numerical and formulation sufficiency

Module J requires convergence under independent variation of:

- mode and wave-number support;
- angular-multipole support;
- time/scale sampling;
- covariance basis and recursive-depth truncation;
- integration rules, tolerances, and precision;
- transfer interpolation;
- factorization method;
- ensemble size and seed families;
- finite volume, boundary, mesh/particle resolution, and long-mode treatment;
- linearity witnesses.

At least two materially independent formulations reproduce the core result, including:

- covariance contraction versus direct ensemble evolution;
- eigendecomposition versus Cholesky/square-root factorization where lawful;
- Fourier/harmonic versus configuration-space reconstruction;
- independent projection quadrature or field generator.

A numerical pass is not a physical theorem, but numerical failure blocks the affected claim.

---

## 31. Stability, causality, and pathology rules

Module J detects and classifies:

- negative covariance eigenvalues;
- divergent infrared or ultraviolet power;
- nonintegrable projection kernels;
- unstable transfer amplification;
- gauge singularities or pure-gauge variance;
- inherited acausal propagation;
- spurious parity/helicity artifacts;
- aliasing and finite-volume contamination;
- discontinuous transfer/background interpolation;
- seed collisions;
- ill-conditioned covariance factorization;
- negative auto-spectra beyond numerical tolerance;
- artificial growth from normalization;
- false nonlinear promotion caused by numerical error.

Pathological branches are obstructed. Upstream physics, covariance, modes, or transfer responses are not retuned to hide the pathology.

---

## 32. Intrinsic signature registry

Before public comparison, Module J freezes signatures including:

- covariance rank and eigenmode structure;
- scalar/vector/tensor and parity/helicity cross blocks;
- route-memory and common-ancestry correlations;
- compression-relic clustering transitions;
- dissipative-tail growth, drag, or metric-slip structure;
- acoustic, damping, drag, and free-streaming features;
- branch-dependent growth and projected-source structure;
- nonlinear-promotion surface morphology;
- finite-volume phase/covariance invariants.

Each signature records physical and triadic origin, mode/route ancestry, sign or phase, scale/time domain, uncertainty, numerical and ablation robustness, downstream pathway, and explicit falsifier.

No signature is invented after a mismatch.

---

## 33. Complete Module J → K parent

For admitted branch and realization \(j\), define

\[
\boxed{
P_{J\to K}(j)
=(\mathrm{Identity}_J,\mathrm{Background}_J,\mathrm{Clock}_J,
\mathrm{Coordinate}_J,\mathrm{Geometry}_J,\mathrm{Boundary}_J,
\mathrm{ModeBasis}_J,\mathrm{PrimordialCovariance}_J,
\mathrm{HigherMomentStatus}_J,\mathrm{TransferIdentity}_J,
\mathrm{LinearSpectra}_J,\mathrm{Growth}_J,\mathrm{SpeciesFields}_J,
\mathrm{SectorFields}_J,\mathrm{MetricFields}_J,
\mathrm{RadiationFields}_J,\mathrm{NeutrinoFields}_J,
\mathrm{CompressionRelic}_J,\mathrm{DissipativeTail}_J,
\mathrm{Stress}_J,\mathrm{Constraint}_J,\mathrm{FiniteVolume}_J,
\mathrm{SeedPhase}_J,\mathrm{Ensemble}_J,
\mathrm{LinearityDomain}_J,\mathrm{PromotionSurface}_J,
\Sigma_J,M_J,\mathrm{Anc}_J,\mathrm{Restart}_J).
}
\]

Module K receives complete nonlinear-entry data. It may not infer or replace missing phase, normalization, gauge, species, sector, stress, covariance, route, branch, memory, ancestry, or field information.

---

## 34. Triad-and-kernel necessity

| Removal or corruption | Necessary consequence |
|---|---|
| remove CIF | no complete mode, route, correlation, phase, branch, ensemble, or promotion possibility space |
| remove QV | no physical covariance qualification, positivity, symmetry, contraction, field generation, or linearity decision |
| remove RFL | no stable covariance, spectra, growth, phase identity, field state, promotion surface, or K export |
| reorder the triad | admitted statistical possibilities, physical qualification, and stabilized linear universe become type-inconsistent |
| remove recursive kernel | shell/route spectral weighting, finite tails, memory, promotion, and ancestry lose their generator |
| remove terminal N-body completion | multi-mode covariance, cross-sector fields, realization, and nonlinear-entry relations are incomplete |
| remove mode independence | covariance rank and Module J physical content can be falsely reduced |
| remove common ancestry | cross-sector covariance becomes arbitrary or disconnected |
| remove PSD condition | generated variances and ensembles can become nonphysical |
| force homogeneity/isotropy/parity | branch, defect, field, helicity, and memory signals can be erased |
| import a public power law | primordial statistics cease to be generated by RFC |
| modify H[I] during contraction | spectra become fitted response products rather than consequences of frozen physics |
| remove direct ensemble check | transfer-contraction errors and phase defects can remain hidden |
| replace the linearity map with one cutoff | species-, route-, time-, and error-dependent failure is erased |
| omit phase/seed identity | Module K cannot reproduce the same physical realization |

---

## 35. Completion and claim boundary

Module J establishes at its declared scope:

- immutable consumption of the Module I and H[I] parents;
- explicit CIF/QV/RFL specialization of the enhanced Module A kernel;
- a complete independent primordial mode basis;
- declared random variables, phase, symmetry, and higher-moment status;
- an endogenous route/shell/memory/common-ancestry covariance generator;
- Hermiticity, positive semidefiniteness, dimensional consistency, finite admitted power, physical quotient, and branch closure;
- scalar/vector/tensor, parity/helicity, entropy, compensated, relative-sector, dark-sector, field, defect, route-memory, and cross-mode blocks where active;
- immutable transfer-covariance contraction;
- complete primary linear radiative spectra and full species/sector/metric spectrum matrices;
- scale- and time-resolved growth and projected source histories;
- gauge, conservation, constraint, reality, and direct/transfer closure;
- finite-volume field generation, covariance recovery, ensemble identity, uncertainty, identifiability, and restart state;
- a componentwise physical linearity domain and nonlinear-promotion surface;
- the complete `P_J->K` parent.

Module J does not claim:

- nonlinear collapse, shell crossing, caustics, or virialized structures;
- halos, mergers, nonlinear lensing, or truth lightcones;
- gas cooling, stars, feedback, enrichment, or galaxies;
- public primordial amplitude, tilt, running, tensor ratio, transfer, CMB, matter, or growth agreement;
- observer, instrument, foreground, or likelihood closure;
- terminal Big-Rip dynamics;
- empirical validation.

Within these boundaries, **Module J is complete and frozen**. Module K is the active child.