# Module M — Stellar and Explosive Nucleosynthesis, Chemical Evolution, Dust, and Composition Return

## 1. Scientific boundary and completion status

Module M consumes the sealed first-pass stellar parent `L^(0)`, the immutable primordial nuclear seed from Module E, the microscopic and thermal laws of Modules C–D, and the generated nonlinear transport environment of Module K. It derives the nuclear and chemical descendants of generated stellar, binary, compact-object, wind, collapse, explosion, merger, ejecta, and transport histories.

The exact parent is

\[
P_{M,\mathrm{in}}^{(n)}=
\big(P_{L\to M}^{(n)},P_E^{\mathrm{prim}},P_{CD}^{\mathrm{microthermal}},P_K^{\mathrm{env},(n)}\big).
\]

The late-universe recurrence is

\[
K^{(n)}\longrightarrow L^{(n)}\longrightarrow M^{(n)}
\longrightarrow K^{(n+1)}.
\]

Module M closes the constitutive law, typed state, conservative returns, and lawful recurrence classification. It does not claim public abundance agreement, public stellar-yield agreement, or a completed empirical galactic-chemical-evolution validation. Those remain Module P work after the generated universe is frozen in Module O.

The Module M theorem target is:

> From generated stellar and compact-event trajectories and immutable upstream physics, RFC constructs an adaptive isotope- and nuclear-state-resolved reaction system, derives forward and reverse route rates from the same microscopic transition law, evolves hydrostatic and explosive burning with exact carrier and energy ledgers, partitions event-resolved yields, propagates radioactive descendants, transports composition without loss of isotope or source identity, derives dust and composition-dependent microphysics, returns changed physical sources to K and L, and classifies the resulting K–L–M recurrence without using public yields or abundance targets as generators.

---

## 2. Immutable sources and ownership

### 2.1 Module L owns trajectories

Module L owns:

- stellar, binary, compact-object, collapse, explosion, merger, wind, transfer, and ejecta identity;
- zone and mass-coordinate histories;
- temperature, density, entropy, electron-fraction, neutrino, radiation, mixing, shock, expansion, fallback, retention, escape, and host trajectories;
- mechanical energy, momentum, angular momentum, and remnant state;
- event timing, branch, covariance, memory, ancestry, and restart state.

A trajectory not generated and sealed by L is inadmissible for an M yield claim.

### 2.2 Module M owns nuclear and chemical descendants

Module M owns:

- nuclear carriers, excited states, properties, and provenance;
- reaction and decay hypergraphs;
- forward/reverse fluxes and equilibrium limits;
- isotope abundance and nuclear-energy evolution;
- hydrostatic, mixed, explosive, wind, transfer, merger, and decay processing;
- total, net, retained, fallback, escaped, radioactive, and dust-bearing yields;
- enrichment parcels, isotope transport, chemical inhomogeneity, and source attribution;
- dust and solved molecular carriers;
- composition-dependent cooling, heating, opacity, emissivity, ionization, radiation coupling, and stress-energy return;
- the return packet to K/L and K–L–M recurrence classification.

### 2.3 Manuscript-use boundary

The W/X/X1 nucleosynthesis suite and W2 Li7 closeout are inherited as triadic mechanism, proxy, ablation, and no-retune evidence. They are not silently promoted into a physical stellar reaction network. In particular:

- the W2 channel-specific CIF/QV/RFL weighting records why scalar triad compression is forbidden;
- the W3/X grouped cascade records source-family and recursive-route requirements;
- the W4/X1 element screen records that spallation and high-Z rarity channels cannot be erased without changing protected outputs;
- comparison residuals, solar patterns, and proxy scores are excluded from Module M generation.

---

## 3. Enhanced completed Module A kernel

Every Module M object specializes the enhanced completed Module A kernel

\[
\widehat{\mathcal K}_A(\tau)
=\sum_{j,[r]}\delta^{-j}e^{-\alpha j\tau}
W_{j,[r]}(\tau)\Phi_{j,[r]}(\tau),
\]

including:

- the ordered `(CIF,QV,RFL)` roles and First Action `QV(CIF)->RFL`;
- directed routes and conjugate/reverse routes;
- local witnesses, event lift, branch alternatives, obstruction, dormancy, promotion, refinement, reopening, and no-loss memory;
- protected identity, recursive depth, bounded tails, covariance, uncertainty, ancestry, and restart;
- terminal finite-N relational completion.

The terminal N-body carrier remains active in nuclear many-body states, stellar zones, ejecta parcels, enrichment carriers, dust populations, host fields, and source ancestry. It cannot be replaced by an empirical yield label.

---

## 4. Module M triadic specialization

\[
CIF_M=\Phi_M\big(P_{M,\mathrm{in}},\widehat{\mathcal K}_A,M_{KLM}\big),
\]

\[
QV_M=\Psi_M\big(CIF_M,P_{M,\mathrm{in}},\widehat{\mathcal K}_A\big),
\]

\[
RFL_M=\Omega_M\big(QV_M,P_{M,\mathrm{in}},\widehat{\mathcal K}_A\big),
\]

\[
H_M=\Gamma_M\big(CIF_M,QV_M,RFL_M,P_{M,\mathrm{in}},\widehat{\mathcal K}_A\big).
\]

- `CIF_M` opens nuclear carriers, reaction/decay routes, burning regimes, source classes, phase carriers, dust/molecular branches, promotions, replays, and return branches that are compatible with the sealed parent.
- `QV_M` supplies the witnessed physical transformations: nonzero transition amplitude, threshold satisfaction, selection rules, forward/reverse pairing, conservation, positivity, trajectory support, transport support, and recurrence qualification.
- `RFL_M` stabilizes the accepted isotope, nuclear-energy, yield, radioactive, enrichment, dust, composition-operator, covariance, memory, ancestry, restart, and K/L-return state.

The triad is operational. It is not a scalar multiplier and may not be collapsed into a fitted abundance correction.

---

## 5. Complete typed state

The complete state is

\[
S_M(t)=\big(
\mathcal N_M,\mathcal P_M,\mathcal H_M^{\rm nuc},Y_M,E_M^{\rm nuc},
\mathcal Y_M,\mathcal D_M^{\rm rad},\mathcal E_M^{\rm enrich},
X_M^{\rm chem},\mathcal G_M^{\rm dust},\mathcal Q_M^{\rm mol},
\mathcal O_M^{\rm comp},\mathcal A_M^{\rm src},
R_{M\to KL},\Sigma_M,\mathfrak M_M,\mathrm{Anc}_M,\mathrm{Restart}_M
\big).
\]

Every field declares:

- physical meaning and units;
- frame, resolution, representation, and active domain;
- source ownership and governing law;
- conservation and constraint role;
- branch, event, route, object, scale, and recurrence identity;
- uncertainty and covariance;
- promotion, refinement, reopening, replay, and restart state;
- manuscript and derivation provenance;
- whether it is directly derived, a controlled effective interface, or benchmark-only.

---

## 6. Nuclear carrier and property registry

A nuclear carrier is

\[
\mathcal N_a=(Z_a,A_a,\xi_a,m_a,B_a,J_a^{\pi},
\{E_{a\mu}\},G_a,T_a,\Gamma_a,\mathcal D_a,
\{S_{a\chi}\},\Sigma_a,\mathrm{Anc}_a).
\]

Here `xi_a` identifies the nuclear state, `B_a` is binding, `E_{a mu}` are admitted excitations, `G_a` is the partition carrier, `T_a` and `Gamma_a` are lifetime/width information, `D_a` is the decay-route set, and `S_{a chi}` are separation or channel thresholds.

The registry is constructed from the inherited microscopic many-body law. For each admitted nucleon content and symmetry sector, solve the controlled finite representation

\[
H^{\rm nuc}_{A,Z}(P_C,P_D,\widehat{\mathcal K}_A)\,|a,\mu\rangle
=E_{a\mu}|a,\mu\rangle.
\]

Carrier admission requires:

1. a normalized state or declared resonance representation;
2. conserved baryon and charge labels;
3. allowed spin/parity/internal symmetry labels;
4. a finite uncertainty and validity domain;
5. a route from the primordial seed or an admitted reaction parent;
6. a nonduplicated identity and restart representation.

Every property is marked as one of:

- `RFC_DERIVED`;
- `RFC_CONTROLLED_EFFECTIVE`;
- `FROZEN_INTERFACE_WITH_UNCERTAINTY`;
- `BENCHMARK_ONLY_EXCLUDED_FROM_GENERATION`.

No hidden nuclear table is permitted.

---

## 7. Directed reaction and decay hypergraph

The active nuclear network is

\[
\mathcal H_M^{\rm nuc}=(V_M,E_M^{+},E_M^{-},E_M^{\rm dec},N_M,W_M).
\]

`V_M` contains nuclear states and required elementary carriers. Each forward route `r` has a conjugate or declared irreversible-open-system partner, a stoichiometric column `N_{:r}`, and a witness packet `W_r`.

For conserved baryon and charge vectors `a` and `z`, internal routes satisfy

\[
a^{\mathsf T}N_M=0,
\qquad
z^{\mathsf T}N_M=0.
\]

Additional exact conserved carriers are encoded as left-null vectors of `N_M`. Lepton, neutrino, photon, and environmental exchange channels are explicit rather than hidden in an apparent violation.

A route is admitted only when:

- reactants and products exist in the typed registry;
- the transition amplitude is nonzero under the inherited symmetry/selection law;
- threshold and generated environment support are satisfied;
- all exchanged carriers and energy channels are identified;
- forward/reverse or decay/future-decay closure exists;
- uncertainty, route ancestry, source trajectory, and validity domain are attached.

Required families are activated only when witnessed: captures, fusion, exchange, photodisintegration, beta/weak capture, neutrino-induced routes, spontaneous and induced decay, fission/fragment routes, spallation, and nonthermal reactions.

---

## 8. Rates from microscopic transition routes

For a route `r: I_r -> F_r`, the forward event density is generated from the same inherited transition operator used to define the channel:

\[
J_r^+[f]=
\int d\Pi_{I_r}\,d\Pi_{F_r}\,
(2\pi)\,\delta_{\rm cons}(P_I-P_F)
\left|\langle F_r|\mathcal T_r|I_r\rangle\right|^2
\prod_{i\in I_r}f_i
\prod_{j\in F_r}(1+\eta_j f_j).
\]

The reverse route uses the conjugate amplitude and the same state normalization:

\[
J_r^-[f]=
\int d\Pi_{F_r}\,d\Pi_{I_r}\,
(2\pi)\,\delta_{\rm cons}(P_F-P_I)
\left|\langle I_r|\mathcal T_r^\dagger|F_r\rangle\right|^2
\prod_{j\in F_r}f_j
\prod_{i\in I_r}(1+\eta_i f_i).
\]

Medium corrections, screening, neutrino occupation, electromagnetic fields, and plasma state enter only through the frozen C–D laws and generated L/K environment.

In a thermal reversible domain, microreversibility gives

\[
\frac{J_r^+}{J_r^-}
=\exp\!\left[-\beta\,\Delta_r\mu\right],
\qquad
\Delta_r\mu=N_{:r}^{\mathsf T}\mu.
\]

Forward and reverse rates are therefore not independent fit parameters. Public reaction-rate fits may later benchmark the frozen law but may not generate it.

---

## 9. Abundance master equation and positivity

Let `Y_a` be abundance per declared baryonic normalization. The reaction system is

\[
\dot Y=N_M\,\mathcal J(Y,\Theta_L,\Theta_D,\Theta_K),
\qquad
\mathcal J_r=J_r^+-J_r^-.
\]

Equivalently, the event-count representation is a continuous-time Markov jump system on nonnegative integer carrier populations. The deterministic abundance equation is its controlled mean-field or large-carrier projection.

The physical integrator must preserve:

- `Y_a >= 0`;
- exact left-null invariants;
- baryon normalization;
- charge and exchanged-carrier accounting;
- event chronology and branch identity;
- stiffness stability and exact restart;
- covariance and route ancestry.

A negative abundance or a conservation residual beyond the declared bound is an obstruction, not a value to clip silently.

---

## 10. Rest-mass, binding, thermal, photon, and neutrino closure

For each route, define the generated release

\[
Q_r=\sum_{i\in I_r}m_i-\sum_{j\in F_r}m_j.
\]

The coupled energy law is

\[
\frac{dE_M}{dt}
=\sum_r Q_r\mathcal J_r
+\dot E_{\rm weak}
+\dot E_{\rm decay}
-\dot E_{\nu,\rm esc}
+\dot E_{\gamma,\rm abs/em}
+\dot E_{\rm transport}.
\]

Each term is paired exactly once with L or K:

\[
\Delta E_{\rm rest}+\Delta E_{\rm bind}+\Delta E_{\rm thermal}
+\Delta E_{\gamma}+\Delta E_{\nu}+\Delta E_{\rm kinetic}
+\Delta E_{\rm remnant}+\Delta E_{\rm escaped}=\epsilon_E.
\]

Momentum and angular momentum carried by emitted particles, photons, neutrinos, ejecta, fallback, and remnants are retained in the shared event packet.

---

## 11. Equilibrium and free-energy limits

For a closed reversible subnetwork, the equilibrium state satisfies

\[
N_M\mathcal J(Y_{\rm eq})=0,
\qquad
N_M^{\mathsf T}\mu(Y_{\rm eq})=0
\]

on active routes.

The reaction entropy production is

\[
\sigma_M=
\sum_r\big(J_r^+-J_r^-\big)
\ln\!\frac{J_r^+}{J_r^-}\ge0.
\]

Thus the appropriate free-energy functional decreases until the detailed-balance manifold or a boundary/open-system steady state is reached. Nuclear statistical equilibrium, quasi-statistical equilibrium, Saha-like limits, weak equilibrium, and low-/high-density limits are controlled reductions of the same network, not separately tuned models.

Switching between full and reduced networks requires shared-state agreement and a bounded reconstruction error.

---

## 12. Adaptive network growth and completeness

The active vertex/edge sets `(V_q,E_q)` are finite truncations of the admitted completed-shell carrier. A missing route or carrier is promoted when any protected omission indicator exceeds its componentwise tolerance:

\[
\epsilon_q^{\rm omit}=
\max_{p\in\mathcal P_M}
\frac{|\Delta_p(V_{q+1},E_{q+1};V_q,E_q)|}
{s_p}>\tau_p.
\]

Protected outputs include:

- isotope abundances and element projections;
- nuclear energy and neutrino loss;
- total/net/retained/fallback/escaped/radioactive yields;
- daughter-chain inventories;
- source-attribution fractions;
- dust-bearing mass;
- cooling, opacity, emissivity, and radiation coupling;
- covariance and recurrence-return sensitivities.

Promotion triggers include flux, energy, covariance, reverse-route closure, daughter necessity, waiting-point/branch sensitivity, fission-fragment support, future radioactive relevance, source attribution, and composition-operator sensitivity.

Pruning is allowed only when every protected component remains within its own bound and the omitted state can be reopened exactly or with a declared reconstruction bound. An aggregate score cannot hide a failed isotope or source family.

---

## 13. Hydrostatic burning and coupled mixing

For stellar zone coordinate `m`,

\[
\partial_t Y_a
+\partial_m\big(F_{a}^{\rm adv}+F_a^{\rm mix}\big)
=\sum_rN_{ar}\mathcal J_r+S_a^{\rm transfer}.
\]

The corresponding zone energy equation is coupled to L through the shared nuclear-power interface. Physically activated branches may include proton-chain, CNO-family, hot-breakout, helium/triple-alpha, alpha-capture, carbon, neon, oxygen, silicon, weak-processing, neutron-source, quasi-equilibrium, and shell/core routes.

Convection, semiconvection, rotation, shear, entrainment, diffusion, thermohaline-like transport, ingestion, binary mixing, merger mixing, and shock mixing are admitted only when supplied or witnessed by L. Mixing and burning preserve isotope mass, energy, zone identity, and ancestry.

Operator splitting is allowed only after an overlap check against a coupled formulation in the claimed regime.

---

## 14. Nonterminal stellar and binary processing

The same reaction law acts on generated histories for:

- steady winds and eruptive loss;
- stripping and mass transfer;
- common-envelope ejection;
- stellar collisions and mergers;
- accretion and surface pollution;
- dredge-up, shell flashes, and generated runaways.

Each event retains donor, recipient, processed layer, time, composition, mass, energy, momentum, angular momentum, route, uncertainty, and ancestry. No empirical source-class yield is substituted for the event solution.

---

## 15. Explosive and compact-event nucleosynthesis

For each generated event `e`, Module M solves

\[
\dot Y_e(t)=N_e\mathcal J_e\big(Y_e;T_e(t),\rho_e(t),s_e(t),
Y_{e}^{\rm electron}(t),\Phi_{\nu,e}(t),B_e(t),\ldots\big)
\]

on the exact L trajectory or a controlled trajectory representation with a quantified approximation error.

Activated branches may include thermonuclear explosions, core-collapse trajectories, compact mergers, neutron-rich or proton-rich ejecta, neutrino-processed outflows, photodisintegration-dominated events, fission recycling, spallation, and nonthermal processing.

A named source family is not enough. Every heavy-element or rare route requires an actual environment, admitted path, waiting-point/branch handling, freeze-out history, daughter contract, uncertainty, and source ancestry.

---

## 16. Event-resolved yield partition

For event `e` and carrier `a`,

\[
Y_{e,a}^{\rm total}
=Y_{e,a}^{\rm retained}
+Y_{e,a}^{\rm fallback}
+Y_{e,a}^{\rm escaped},
\]

with separate derived views

\[
Y_{e,a}^{\rm net},\quad
Y_{e,a}^{\rm radioactive},\quad
Y_{e,a}^{\rm dust-bearing}.
\]

These are not independent tables. They are projections of the same zone-, trajectory-, phase-, velocity-, host-, covariance-, and ancestry-resolved event solution. The dust-bearing view is a typed phase subset and cannot exceed the corresponding admitted material carrier.

---

## 17. Radioactive descendants and delayed energy

For a daughter network with generator `Q_D`,

\[
\dot y_D=Q_Dy_D,
\qquad
y_D(t)=e^{Q_D(t-t_0)}y_D(t_0).
\]

Internal daughter transfer satisfies

\[
\mathbf 1^{\mathsf T}Q_D=0
\]

when all mass-number-preserving daughters and emitted carriers are represented in the complete ledger. Open escaped carriers are explicit boundary terms.

Each unstable state carries:

- parent and daughter identities;
- branch fractions, widths, and decay times;
- emitted particles, photons, and neutrinos;
- deposited and escaping energy;
- ionization, heating, dust/molecule incorporation, source, phase, transport, host, covariance, and ancestry;
- an exact checkpoint/restart state or complete future-decay contract.

The semigroup law

\[
e^{Q_D(t_2-t_0)}=e^{Q_D(t_2-t_1)}e^{Q_D(t_1-t_0)}
\]

guarantees restart consistency.

---

## 18. Enrichment parcels and conservative transport

Each enrichment carrier is

\[
\mathcal E_p=(Y_p,m_p,x_p,v_p,\mathrm{phase}_p,\mathrm{ion}_p,
T_p,\mathcal G_p^{\rm dust},\mathcal Q_p^{\rm mol},e_p,\Sigma_p,\mathrm{Anc}_p).
\]

For isotope density `rho Y_a`,

\[
\partial_t(\rho Y_a)
+\nabla\cdot(\rho Y_av+J_a^{\rm diff}+J_a^{\rm turb})
=S_a^{\rm event}+S_a^{\rm decay}+S_a^{\rm phase}.
\]

Internal phase exchange is antisymmetric between donor and recipient. Advection, diffusion, turbulent mixing, shocks, buoyancy, condensation, winds, fountains, jets, retention, escape, stripping, reaccretion, cloud incorporation, and star incorporation are evolved in the generated K/L environment.

Integrated isotope change equals explicit boundary and source flux. Numerical mixing may not create or erase isotopes or source identity.

---

## 19. Chemical inhomogeneity and source ancestry

Define the ancestry tensor `A_{a p e l}` as the fraction of carrier `a` in parcel/field `p` descending from source event `e` and stellar/compact lineage `l`. It evolves with the same conservative transport operator as the isotope carrier:

\[
\partial_t(\rho Y_aA_{apel})+\nabla\cdot F_{apel}=S_{apel}.
\]

For each nonzero carrier,

\[
\sum_{e,l}A_{apel}=1.
\]

This preserves local enrichment bubbles, incomplete mixing, rare-event signatures, source clustering, phase segregation, gradients, enrichment ages, host variance, abundance covariance, and source-family fractions.

The required ancestry query remains

```text
carrier -> transport -> ejecta parcel -> source event
        -> stellar/compact lineage -> primordial seed
        -> Big Implosion -> enhanced triad kernel.
```

---

## 20. Dust and molecular boundary

Dust states are admitted from explicit material and surface routes:

\[
\dot G_s=
\mathcal C_s^{\rm nucleate}+
\mathcal C_s^{\rm grow}+
\mathcal C_s^{\rm coag}
-\mathcal D_s^{\rm shatter}
-\mathcal D_s^{\rm sputter}
-\mathcal D_s^{\rm sublimate}
+\mathcal T_s^{\rm phase}.
\]

Dust mass is transferred from and returned to the gas/isotope ledger exactly once. Each carrier retains composition, size/state distribution at claimed fidelity, source event, phase, host, opacity/emissivity contribution, covariance, route, and transport ancestry.

Molecular carriers are admitted only when their explicit network, generated gas/dust/radiation/shielding/nonthermal inputs, conservation, energy closure, and effect on cooling, fragmentation, emission, or later physics are established.

Planetary, prebiotic, biological, and cognitive chemistry are outside Module M.

---

## 21. Composition-dependent microphysics

From the same typed carrier states and inherited microscopic transition law, Module M constructs

\[
\mathcal O_M^{\rm comp}=
\{P_{\rm EOS},\Lambda,\Gamma,\kappa_\nu,\eta_\nu,
\chi_{\rm ion},G_{\rm rad}^{\mu},Q_{\rm radio},T_{\rm comp}^{\mu\nu}\}.
\]

Representative operators are

\[
\kappa_\nu=\sum_c n_c\,\sigma_{c,\nu}^{\rm abs/scat},
\qquad
\eta_\nu=\sum_c n_c\,j_{c,\nu},
\]

\[
\Lambda-\Gamma
=\sum_{r}\Delta E_r\,\mathcal J_r
+\sum_d\Delta E_d\,\mathcal J_d
+\mathcal C_{\rm dust}+\mathcal C_{\rm mol}.
\]

Absorption, emission, scattering, heating, cooling, ionization, and stress-energy terms are paired with the radiation/matter equations in L and K. Passivity and detailed-balance domains require nonnegative absorption and nonnegative dissipative quadratic forms.

No public cooling curve, opacity table, or dust-to-metal law is relabeled as RFC-derived.

---

## 22. Lineage-derived rates and delay-time distributions

For source class `c`, the event measure is the pushforward of generated births and branch evolution:

\[
\mu_c^{\rm event}
=(\mathcal T_c)_*\mu_L^{\rm birth},
\]

where `T_c` maps a generated stellar/binary lineage to its witnessed event time, environment, branch, and ejecta state. The delay measure is

\[
D_c(\Delta t)=
\int\delta\!\left(\Delta t-[t_c(\ell)-t_{\rm birth}(\ell)]\right)
\,d\mu_L^{\rm birth}(\ell).
\]

Thus source rates and delay-time distributions are outputs of L/M lineage physics. They are never empirical inputs to generation.

---

## 23. Return to Modules K and L

Module M exports

\[
R_{M\to KL}^{(n)}=\big(
X_{\rm isotope/element},X_{\rm gas},X_{\rm star},X_{\rm remnant},
X_{\rm dust},\Lambda,\Gamma,\kappa,\eta,G_{\rm rad},Q_{\rm radio},
\dot M_{\rm return},\Delta T^{\mu\nu},\mathcal E_{\rm transport},
\mathrm{ChangedDomain},\Sigma_M,\mathfrak M_M,\mathrm{Anc}_M,\mathrm{Restart}_M
\big).
\]

Every return has one owner and one recipient. K and L may evolve the changed physical state forward but may not alter M nuclear history, double count returned mass/energy, or select a branch from public residuals.

Replay begins at the earliest causally affected interval and follows only descendants of changed composition, dust, opacity, cooling, radioactive, or stress-energy carriers. Modules A–J remain frozen.

---

## 24. K–L–M recurrence and lawful classification

Let the protected late-universe state be

\[
X_n=(K^{(n)},L^{(n)},M^{(n)}),
\]

and define the composed recurrence

\[
X_{n+1}=\mathcal R_{KLM}(X_n)
=\mathcal M\circ\mathcal L\circ\mathcal K(X_n).
\]

The branch is classified by its actual generated return operator:

1. **Fixed point:** `X*=R_KLM(X*)`; local contraction is certified when the spectral radius of the linearized return satisfies `rho(DR_KLM[X*])<1` in the protected norm.
2. **Bounded cycle:** `R_KLM^p(X*)=X*` with all intermediate states distinct and the cycle ledger/restart state closed.
3. **Slowly evolving attractor:** invariant attracting set with bounded protected drift and explicit secular variables.
4. **Branch family:** multiple physically distinct witness-supported branches remain separate with normalized internal branch measure and no public-data selection.
5. **Nonconvergence:** unbounded drift, unresolved oscillation, failed closure, or `rho>=1` without a proved nonlinear attracting mechanism is retained explicitly and constrains downstream claims.

No artificial damping may be inserted to force a fixed point. The recurrence classification itself is a valid closed output when every possible generated branch is assigned one of these statuses with its proof witness and restrictions.

---

## 25. Uncertainty and covariance

The covariance is

\[
\Sigma_M=
J_L\Sigma_LJ_L^{\mathsf T}
+J_E\Sigma_EJ_E^{\mathsf T}
+J_{CD}\Sigma_{CD}J_{CD}^{\mathsf T}
+J_K\Sigma_KJ_K^{\mathsf T}
+\sum_q\Sigma_q,
\]

where process terms include nuclear-state, transition, rate, network, mixing, trajectory, event, decay, transport, dust, composition-operator, replay, numerical, and branch contributions.

Covariance remains resolved by isotope, reaction family, zone, trajectory, event, radioactive chain, parcel, phase, host, dust carrier, recurrence branch, and observable projection. Correlations are not replaced by independent error bars.

---

## 26. No-loss promotion, refinement, and restart

Promotion may map isotopes to elements, source families, zones, parcels, hosts, or cosmic fields only when protected isotope, radioactive, source, phase, covariance, and ancestry information remains recoverable or bounded with a reopening rule.

A restart record contains:

- exact active registry/hypergraph version;
- state vector and normalization;
- rate/operator version and precision;
- trajectory time and event bracket;
- daughter, transport, dust, and return states;
- recurrence iteration and branch identity;
- covariance, memory, ancestry, and unresolved obstruction state.

Restarted evolution must reproduce direct evolution within the declared componentwise bounds.

---

## 27. Exact export to Module N

After recurrence classification, Module M contributes

\[
P_{M^*\to N}=\big(
\mathrm{Identity}_{M^*},\mathrm{KLMClosureState},
\mathrm{IsotopeInventory},\mathrm{NuclearStateRegistry},
\mathrm{ElementInventory},\mathrm{RadioactiveRegistry},
\mathrm{ChemicalPhaseRegistry},\mathrm{MolecularRegistry},\mathrm{DustRegistry},
\mathrm{SourceFamilyRegistry},\mathrm{YieldRegistry},
\mathrm{EnrichmentRegistry},\mathrm{RetentionEscapeReaccretion},
\mathrm{MixingTransport},\mathrm{HostFields},\mathrm{CosmicFields},
\mathrm{ChemicalTagging},\mathrm{CompositionCooling},
\mathrm{CompositionOpacity},\mathrm{RadioactiveHeating},
\mathrm{NuclearPathAncestry},\mathrm{ChemicalTransportAncestry},
\Sigma_M,\mathfrak M_M,\mathrm{Anc}_M,\mathrm{Restart}_M
\big).
\]

Module N receives a complete typed chemical universe and a classified K–L–M parent, not a metallicity scalar or detached yield catalogue.

---

## 28. Intrinsic signatures and falsifiers

Before public comparison, freeze:

- source-resolved isotope correlations;
- branch-specific radioactive ratios and delayed-power structures;
- inhomogeneous enrichment and rare-event memory;
- dust/phase-dependent covariance;
- environment-dependent source attribution;
- composition feedback on collapse, stars, nonlinear geometry, and lensing;
- Big-Implosion and triad ancestry through nuclear/chemical memory.

Principal falsifiers are:

- failed network completeness;
- failed baryon, charge, isotope, mass-energy, momentum, or angular-momentum closure;
- forward/reverse inconsistency;
- inability to derive yields from generated trajectories;
- unresolved daughter chains;
- nonconservative transport or phase exchange;
- lost source ancestry;
- double-counted K/L returns;
- unclassified recurrence;
- dependence on public yield or abundance targets.

---

## 29. Strongest supported and unsupported claims

### Strongest supported claim

Within the declared finite, typed, internally generated RFC representation, Module M supplies one conservation-complete constitutive construction from sealed stellar/compact trajectories to adaptive nuclear networks, event-resolved isotope yields, radioactive descendants, chemical transport, dust, composition-dependent return operators, and lawful K–L–M recurrence classification. The construction preserves triadic descent, no-retune generation, source ancestry, covariance, replay locality, and exact downstream interfaces.

### Not established here

Module M does not establish:

- empirical correctness of RFC nuclear masses or reaction rates;
- observed stellar, supernova, merger, solar, galactic, or intergalactic abundance agreement;
- a public-code BBN or stellar-network validation;
- a unique realized numerical cosmic chemical history without an instantiated full parent trajectory set;
- Module N manifested-universe assembly;
- Module O immutable freeze;
- Module P empirical validation;
- Module Q terminal Big-Rip closure.

Those boundaries are preserved rather than hidden.

---

## 30. Module M completion statement

Module M is complete and frozen at its declared derivational and finite-verification scope when:

1. the exact L/E/C/D/K parents are immutable and typed;
2. nuclear carriers and reaction/decay routes are admitted by the inherited microscopic law;
3. forward/reverse closure, positivity, invariants, energy, yields, daughters, transport, dust, composition operators, covariance, memory, ancestry, and restart are explicit;
4. K and L can re-ingest every changed source without double counting;
5. every generated recurrence branch is classified as fixed, cyclic, attracting, branched, or explicitly nonconvergent;
6. the complete `P_M*->N` packet exists;
7. public targets remain outside generation.

These conditions are now the frozen Module M law. Module N is the next active repair frontier.
