# Module I — Realized RFC Background Geometry, Expansion, Horizons, and Distance Map

## 1. Scientific boundary

Modules A through G and Module Hᵁ are complete and frozen. Module I consumes two immutable parents:

\[
P_{I,\mathrm{in}}
=\left(P_G^{\mathrm{phys}},P_{H^U}^{\mathrm{op}}\right),
\]

where \(P_G^{\mathrm{phys}}\) is the complete generated physical universe through primordial recombination and \(P_{H^U}^{\mathrm{op}}\) is the frozen background-parameterized linear operator, admissible background domain, insertion map, and operator identity.

Module I derives the branch-specific realized background

\[
\mathcal B_I
=(\mathcal G_I,\mathcal C_I,\mathcal S_I,\mathcal X_I,
\mathcal E_I,\mathcal H_I,\mathcal D_I,\mathcal J_I,
\Sigma_I,M_I,\mathrm{Anc}_I,\mathrm{Restart}_I)
\]

and proves

\[
\mathcal B_I\in\mathcal D_{\mathcal B},
\]

where \(\mathcal D_{\mathcal B}\) is the already-frozen Module Hᵁ admissible domain.

The completed Module I theorem is:

> For every admitted dual-parent branch and declared finite relational/geometric refinement, the enhanced completed Module A triad kernel, the frozen Big-Implosion ancestry, and the generated A–G physical state define a constrained relational geometry and sector-source system with a unique branch-specific evolution until a declared event, pathology, or branch boundary. The solution generates mature geometry, physical and conformal clocks, scale and expansion histories, ordinary, radiative, compression-relic, dissipative-tail, relic, field, and defect backgrounds, witnessed inter-sector transfer, curvature, anisotropy, averaging/backreaction, redshift, null or lawful signal propagation, horizons, distances, derivatives, covariance, memory, and ancestry. Every accepted solution preserves the early A–G chronology, satisfies total conservation and geometric constraints, lies inside the frozen Hᵁ domain, and activates H[I] without modifying the frozen operator or importing public expansion, distance, ruler, dark-sector, or normalization data.

The result is complete at a **finite-relational, internally scaled, generated-background scope**. It is not an empirical expansion fit, a measured present-day cosmology, a public distance ladder, a final primordial covariance, or a terminal-cycle calculation.

---

## 2. Immutable dual-parent input

The physical parent contains, branch by branch:

- universe, cycle, branch, and source identity;
- the enhanced completed Module A kernel and terminal N-body carrier;
- Big-Implosion geometry/pregeometry and four-sector common ancestry;
- ordinary, radiative, compression-relic, and dissipative-tail sector states;
- particle, antiparticle, nuclear, atomic, ionic, photon, neutrino, field, and effective-species registries;
- microscopic interactions, decays, collision channels, equations of state, and constitutive operators already generated upstream;
- thermal, nuclear, plasma, recombination, ionization, opacity, visibility, drag, diffusion, and damping histories;
- physical and conformal timing anchors through Module G;
- stress-energy-facing densities, pressures, fluxes, anisotropic stresses, transfer permissions, and active spatial/field states;
- covariance, uncertainty, route, event, scale, branch, memory, ancestry, and restart state.

The Hᵁ operator parent contains:

- \(\mathbb B_{\mathrm{RFC}}[\mathcal B]\), the frozen background-parameterized linear operator;
- \(\mathcal D_{\mathcal B}\), its admissible background domain;
- required variables, units, clocks, derivatives, species, sectors, event surfaces, interpolation rules, and sampling rules;
- pathology exclusions and stability requirements;
- the immutable insertion map;
- the frozen operator identity.

A material missing parent field is an upstream or interface obstruction. Module I may not fill it from a conventional background model.

---

## 3. Enhanced completed Module A kernel

Every Module I construction specializes

\[
\widehat{\mathcal K}_A(\tau)
=\sum_{j=1}^{n_A}\sum_{[r]\in\mathcal A_A(\tau)}
\delta^{-j}e^{-\alpha j\tau}W_{j,[r]}(\tau)\Phi_{j,[r]}(\tau),
\]

including:

- ordered CIF/QV/RFL roles;
- the First Action \(QV(CIF)\to RFL\);
- recursive depth and modal structure;
- directed relations, route classes, and local witnesses;
- event lift, inverse/conjugate routes, and legal incidence;
- branch, obstruction, promotion, refinement, and reopening;
- protected identity, no-loss memory, uncertainty, covariance, and ancestry;
- terminal N-body relational and solution-generating completion.

The N-body carrier is active in geometric constituent relations, connection and curvature construction, source averaging, sector exchange, horizon/signal routing, anisotropic refinement, and branch continuation. Where no Module I witness exists, it remains dormant, information-bearing, unchanged, and nonbackreacting.

---

## 4. Module I triadic specialization

Define

\[
CIF_I
=\Phi_I(P_G^{\mathrm{phys}},P_{H^U}^{\mathrm{op}},\widehat{\mathcal K}_A,M_G),
\]

\[
QV_I
=\Psi_I(CIF_I,P_G^{\mathrm{phys}},P_{H^U}^{\mathrm{op}},\widehat{\mathcal K}_A),
\]

\[
RFL_I
=\Omega_I(QV_I,P_G^{\mathrm{phys}},P_{H^U}^{\mathrm{op}},\widehat{\mathcal K}_A),
\]

\[
H_I
=\Gamma_I(CIF_I,QV_I,RFL_I,P_G^{\mathrm{phys}},P_{H^U}^{\mathrm{op}},\widehat{\mathcal K}_A).
\]

The roles are functional:

- **CIF\(_I\)** opens the lawful geometry, topology, slicing, curvature, anisotropy, constitutive, transfer, averaging, event, horizon, distance, and asymptotic branches admitted by the parents;
- **QV\(_I\)** executes constrained geometric/sector evolution, route admission, branch selection, event localization, pathology rejection, scale promotion, and H-domain qualification;
- **RFL\(_I\)** stabilizes the realized restartable background, distance and horizon map, uncertainty, memory, ancestry, H[I] instantiation state, and Module J export.

No public expansion curve is relabeled as a triadic result.

---

## 5. Finite relational geometry

At each physical time, let \(\mathfrak K_I(t)\) be the finite cell complex promoted from the manifested Module B relational graph by the Module A add-one and refinement laws.

For vertices \(u,v\), define the inherited resistance-distance carrier

\[
d_R^2(u,v;t)
=(e_u-e_v)^\mathsf T L_I^+(t)(e_u-e_v),
\]

where \(L_I\) is the positive relational Laplacian generated by active weighted relations and \(L_I^+\) is its Moore–Penrose inverse on the mean-zero subspace.

The physical edge length is

\[
\ell_e(t)=\ell_C\,\Xi_e\!\left[d_R,\widehat{\mathcal K}_A,
\mathrm{Anc}_e,M_e\right],
\]

where \(\ell_C=\Lambda_C^{-1}\) is the internally generated microscopic length unit and \(\Xi_e>0\) is the witnessed, dimensionless edge realization map. No measured distance is inserted.

A local metric Gram matrix \(h_c\) is reconstructed in each admitted cell \(c\) from its edge-length data. Degenerate or inconsistent cells are obstructed or refined.

---

## 6. Connection, curvature, topology, and continuum carrier

For neighboring cells \(c,c'\), define the relational parallel-transport operator \(U_{cc'}\) as a minimizer of

\[
\mathfrak C[U]
=\sum_{(c,c')}
\omega_{cc'}\|U_{cc'}^\mathsf T h_{c'}U_{cc'}-h_c\|^2
+\lambda_T\|\mathcal T_{cc'}[U]\|^2,
\]

subject to the branch's orientation, signature, and cell-incidence constraints. The first term enforces metric compatibility; the second enforces the generated torsion condition. Multiple minimizers remain distinct connection branches unless no-loss equivalence is proved.

For an oriented face \(f\), curvature is the holonomy

\[
\mathcal R_f
=\log\left(\overrightarrow{\prod_{e\in\partial f}}U_e\right).
\]

The background curvature carrier is the oriented contraction

\[
\mathcal R_I
=\frac{1}{V_I}
\sum_f A_f\langle B_f,\mathcal R_f\rangle,
\]

with face area \(A_f\), oriented bivector \(B_f\), and relational volume \(V_I\).

Topology is determined by the admitted incidence complex and its homology/cohomology. It is not selected to obtain a familiar distance curve.

When a smooth chart is witnessed under refinement, the cell metric, connection, and holonomy define an effective metric \(g_{\mu\nu}\), connection \(\Gamma^\rho_{\mu\nu}\), and curvature tensor. A continuum equation is a derived limit or bounded interface of this finite carrier, not an independent starting cosmology.

---

## 7. Background averaging and no-loss coarse graining

For a relational domain \(\Omega\), define

\[
\langle X\rangle_\Omega
=\frac{1}{V_\Omega}\sum_{c\subset\Omega}V_cX_c.
\]

The background projection records:

- domain and measure;
- slicing and observer congruence;
- retained mean variables;
- unresolved stress and correlations;
- scale dependence;
- covariance;
- reopening map to the cell state.

For expansion \(\Theta\) and shear \(\sigma\), the exact variance/backreaction carrier contains

\[
\mathcal Q_\Omega
=\frac{2}{d_I}
\left(\langle\Theta^2\rangle_\Omega-\langle\Theta\rangle_\Omega^2\right)
-2\langle\sigma^2\rangle_\Omega
+\mathcal C_{\mathrm{rel},\Omega},
\]

where \(\mathcal C_{\mathrm{rel},\Omega}\) contains finite relational correlation terms not represented by continuum moments.

No averaging term is tuned as a residual correction. A homogeneous or isotropic branch is accepted only when \(\mathcal Q_\Omega\), anisotropy, topology, and unresolved-stress effects are derived, retained, or bounded below the declared tolerance.

---

## 8. Complete background state

Use background coordinates

\[
q^A=(\nu,\beta^a,\chi^m),
\qquad
\nu=\log(V_I/V_\star),
\]

where \(\beta^a\) are independent shape/anisotropy/topology coordinates and \(\chi^m\) are active geometric, field, memory, or dark-sector background coordinates.

The full finite state is

\[
X_I
=(q^A,p_A,\{Y_s\},\{\Pi_s\},\mathcal C,\mathcal E,
\mathcal H,\mathcal D,\Sigma,M,\mathrm{Anc}).
\]

Every component carries units, coordinate behavior, source ancestry, evolution law, constraint role, admissible domain, uncertainty, branch, and export status. No claimed quantity depends on an untyped hidden parameter.

---

## 9. Relational geometric action and constrained evolution

The generated finite background action is

\[
\boxed{
S_I=\int d\lambda\left[
 p_A\dot q^A
 +\sum_s\langle\Pi_s,\dot Y_s\rangle
 -N\mathscr C_I
 -\Lambda^\alpha\mathscr C_\alpha
\right],
}
\]

with Hamiltonian constraint

\[
\boxed{
\mathscr C_I
=\frac12p_A\mathfrak G^{AB}(q)p_B
+\mathcal U_{\mathrm{curv}}(q)
+\mathcal U_{\mathrm{rec}}(q,M)
+V_I(q)\rho_{\mathrm{tot}}(q,Y)
=0.
}
\]

Here:

- \(\mathfrak G^{AB}\) is the relational supermetric obtained from the Hessian of the finite geometric action; its volume direction is indefinite while physical shape/field directions satisfy the branch stability conditions;
- \(\mathcal U_{\mathrm{curv}}\) is the holonomy/curvature potential;
- \(\mathcal U_{\mathrm{rec}}\) is the lawful recursive-memory and finite-cell correction potential;
- \(\rho_{\mathrm{tot}}\) is assembled once from all active sectors and interaction carriers without double counting;
- \(\mathscr C_\alpha\) are momentum, charge, incidence, topology, gauge, normalization, and other generated constraints.

Hamilton evolution is

\[
\dot q^A=N\frac{\partial\mathscr C_I}{\partial p_A},
\qquad
\dot p_A=-N\frac{\partial\mathscr C_I}{\partial q^A}
-\Lambda^\alpha\frac{\partial\mathscr C_\alpha}{\partial q^A},
\]

with the corresponding sector equations and constraint multipliers.

This action is the mature background specialization of the finite relational carrier. An Einstein-like or Friedmann-like equation appears only as a derived symmetry/continuum limit of this action under explicitly named assumptions.

---

## 10. Stress-energy and source construction

For each active sector \(s\), let \(E_s[V,h,Y_s]\) be its generated energy functional. Define

\[
\rho_s=\frac{E_s}{V_I},
\qquad
p_s=-\left.\frac{\partial E_s}{\partial V_I}\right|_{Y_s,S_s,\beta},
\]

\[
\pi_s^{ab}
=-\frac{2}{V_I}
\frac{\delta E_s}{\delta h_{ab}}\bigg|_{\mathrm{traceless}},
\qquad
q_s^a=\text{generated energy-flux projection}.
\]

The effective stress carrier is

\[
T_s^{\mu\nu}
=T_s^{\mu\nu}
[\rho_s,p_s,u_s,q_s,\pi_s,Y_s,M_s].
\]

Every term is traced to a frozen parent energy, route, field, or constitutive operator. Interaction energy is represented exactly once in either a sector assignment or a separate interaction carrier.

The geometric source in background coordinate \(q^A\) is

\[
\mathcal S_A
=-\frac{\partial}{\partial q^A}
\left[V_I\rho_{\mathrm{tot}}\right].
\]

---

## 11. Witnessed inter-sector transfer

For a witnessed route \(r:s\to u\), define

\[
\mathcal A_r(t)
=\sum_j\delta^{-j}e^{-\alpha jt}
W_{j,r}(t)\langle u|\Phi_{j,r}|s\rangle
\]

and the generated background transfer current

\[
Q_{s\to u}
=\sum_{r\in\mathcal W_{s\to u}}
\varepsilon_r\,\gamma_r\,\mathcal N_r,
\qquad
\gamma_r\ge0.
\]

The paired current satisfies

\[
Q_{u\leftarrow s}=-Q_{s\to u}
\]

in the total energy ledger. Every current records source, recipient, interaction law, event/continuous status, energy, momentum, entropy, memory, reversibility, route, branch, covariance, and ancestry.

Unwitnessed transfer is exactly zero.

---

## 12. Sector conservation and geometric constraints

For each sector,

\[
\boxed{
\dot\rho_s
+\Theta_I(\rho_s+p_s)
+\sigma_{ab}\pi_s^{ab}
+\nabla_aq_s^a
=Q_s,
}
\]

where \(\Theta_I=\dot V_I/V_I\) and \(\sum_sQ_s=0\).

Therefore

\[
\dot\rho_{\mathrm{tot}}
+\Theta_I(\rho_{\mathrm{tot}}+p_{\mathrm{tot}})
+\sigma_{ab}\pi_{\mathrm{tot}}^{ab}
+\nabla_aq_{\mathrm{tot}}^a=0.
\]

The complete constraint state includes:

- Hamiltonian/energy constraint;
- momentum constraint;
- incidence and topology constraints;
- total energy and momentum conservation;
- sector-transfer zero sum;
- integrated exchange balance;
- event balance;
- entropy production;
- recursive-memory balance;
- no-double-counting residual.

Constraint propagation follows from the canonical action and the generated geometric identity. A growing unexplained residual is an obstruction.

---

## 13. Internal dimensional constitution

The unit map is generated from upstream RFC quantities:

\[
\mathcal U_I
=\mathfrak U_I
(\Lambda_C,RFL_B^{\mathrm{manifest}},T_G,n_{B,G},
\mathfrak G_I,\widehat{\mathcal K}_A).
\]

It supplies internally related units of length, time, energy, temperature, propagation speed, density, pressure, and geometric coupling.

A physical reference event \(\mathcal E_\star\) is selected only from an internally generated branch event, such as a frozen Module G boundary, a later lawful branch event, or a cycle-defined reference. It is not a public present-day calibration.

Scale normalization is therefore a coordinate convention attached to \(\mathcal E_\star\), while dimensionful ratios and propagation laws retain their upstream unit ancestry. No public age, Hubble normalization, distance, or ruler is used.

---

## 14. Early-to-late continuity

Let \(\mathcal I_{\mathrm{ov}}\) be the overlap interval on which Modules B–G already provide a background-facing state. Module I imposes

\[
\boxed{
\mathcal B_I|_{\mathcal I_{\mathrm{ov}}}
=\mathcal B_{B\to G}
+\mathcal R_{\mathrm{ov}},
\qquad
\|\mathcal R_{\mathrm{ov}}\|\le\varepsilon_{\mathrm{ov}}.
}
\]

The overlap compares physical and conformal time, volume/scale, expansion, densities, pressures, temperatures, chemical potentials, entropy, recombination/drag events, sound/drag integrals, transfer currents, curvature, anisotropy, and covariance.

A material failure does not authorize a late Module I correction. It reopens the chain at the earliest physically affected module.

---

## 15. Ordinary and radiative sectors

Module I preserves and evolves the generated inventories of:

- baryons, electrons, residual ions, neutral atoms, primordial isotopes, and any surviving ordinary exotic state;
- photons, neutrinos, other relics, recombination radiation, intrinsic spectral distortions, and generated radiation-like states;
- background-order gravitational-wave, magnetic, defect, or field energy where active.

Their dilution, redshifting, residual decay/conversion, massive-to-relativistic transitions, pressure, sound speed, temperature ratio, anisotropic stress, interaction current, and geometric source follow from their upstream energy/distribution functionals.

No late astrophysical conversion is invented before the K–L–M chain generates it.

---

## 16. Compression-relic background law

Let \(m_C\) be the protected compression-memory carrier inherited from the Big Implosion and refined through Modules C–H. Its energy functional is

\[
\boxed{
E_{\mathrm{comp}}
=\frac12\langle m_C,
\mathcal C_{\mathrm{comp}}(V_I,h,M)m_C\rangle,
}
\]

where \(\mathcal C_{\mathrm{comp}}\succeq0\) descends from the stored nonconstant-mode compression operator and its lawful transport/refinement.

Define

\[
\rho_{\mathrm{comp}}=E_{\mathrm{comp}}/V_I,
\qquad
p_{\mathrm{comp}}
=-\partial E_{\mathrm{comp}}/\partial V_I,
\]

\[
w_{\mathrm{comp}}=p_{\mathrm{comp}}/\rho_{\mathrm{comp}},
\qquad
c_{s,\mathrm{comp}}^2
=\dot p_{\mathrm{comp}}/\dot\rho_{\mathrm{comp}}
\]

where the denominator is nonzero, with the full constitutive derivative used otherwise.

The sector continuity equation includes only witnessed transfer and decay routes. A dust-like branch is derived when \(E_{\mathrm{comp}}\) is approximately volume-independent, shear response is negligible, transfer is bounded, and the Hᵁ perturbative contract is stable. It is not assigned.

The classification “dark-matter-like” is allowed only after positive density, persistence, stable background and perturbation laws, gravitational sourcing, clustering-compatible response, acceptable interaction/free-streaming behavior, and no-double-counting closure are proved.

---

## 17. Dissipative-tail background law

Represent the dissipative-tail memory by generated modes \(z_\sigma\) satisfying

\[
\dot z_\sigma
=-\sigma z_\sigma+J_\sigma[X_I],
\qquad
\sigma\ge0,
\]

with nonnegative kernel measure \(\mu(\sigma)\). Define

\[
\boxed{
E_{\mathrm{tail}}
=\frac12\int d\mu(\sigma)\|z_\sigma\|^2
+\mathcal U_{\mathrm{tail}}(V_I,h,z,M),
}
\]

\[
\rho_{\mathrm{tail}}=E_{\mathrm{tail}}/V_I,
\qquad
p_{\mathrm{tail}}
=-\left.\frac{\partial E_{\mathrm{tail}}}{\partial V_I}\right|_{z,S,\beta}.
\]

The generated entropy production is

\[
\dot S_{\mathrm{tail}}
=\frac1{T_{\mathrm{tail}}}
\int d\mu(\sigma)\,\sigma\|z_\sigma\|^2
+\dot S_{\mathrm{route}}
\ge0
\]

on the dissipative branch.

Its equation of state, sound/propagation behavior, anisotropic stress, transfer current, asymptotics, memory dependence, and terminal-exhaustion interface are outputs.

A nearly persistent tail energy can generate negative pressure because pressure is the volume derivative of the generated energy. Neither \(w=-1\) nor a \(w_0-w_a\) form is inserted.

---

## 18. Generated expansion and acceleration

Define

\[
a_I=(V_I/V_\star)^{1/d_I},
\qquad
H_I=\frac{1}{d_I}\Theta_I,
\]

and decompose the expansion tensor into trace, shear, and vorticity.

On a homogeneous-isotropic continuum branch of the relational action, the Hamiltonian constraint reduces to

\[
\boxed{
H_I^2
=\frac{2\kappa_I}{d_I(d_I-1)}\rho_{\mathrm{tot}}
-\frac{k_I}{a_I^2}
+\mathcal B_I^{\mathrm{avg/mem}},
}
\]

where \(\kappa_I\), \(k_I\), and \(\mathcal B_I^{\mathrm{avg/mem}}\) are generated from the relational supermetric, curvature, averaging, and memory potentials.

The corresponding acceleration relation is

\[
\boxed{
\frac{\ddot a_I}{a_I}
=-\frac{\kappa_I}{d_I(d_I-1)}
\left[(d_I-2)\rho_{\mathrm{tot}}+d_Ip_{\mathrm{tot}}\right]
+\mathcal A_I^{\mathrm{shear/avg/mem}}.
}
\]

These are derived symmetry reductions, not the generative starting equations.

Acceleration is admitted only when the generated pressure, curvature, shear, averaging, memory, or transfer terms satisfy the equation. A dissipative-tail classification is not sufficient by itself.

---

## 19. Background events

Module I localizes generated events as zeros or rank changes of physical functions, including where they exist:

- radiation/ordinary/relic/compression/tail equality or crossover;
- acceleration/deceleration transition;
- transfer onset or shutoff;
- massive/relativistic transition;
- curvature, anisotropy, or backreaction transition;
- branch bifurcation;
- recollapse, bounce, asymptotic, trapped, or anti-trapped transition;
- horizon crossing.

For event function \(F_e(X_I,t)=0\), the event packet is

\[
\mathcal E_e
=(t_e,X_I(t_e),\dot F_e,\mathrm{branch}_e,
\Sigma_e,M_e,\mathrm{Anc}_e).
\]

A tangential or multiple root retains its multiplicity and uncertainty. No event coordinate is assigned from public cosmology.

---

## 20. Time constitution

Module I keeps distinct:

\[
t_{\mathrm{phys}},\qquad
\eta,\qquad
s,\qquad
j,
\]

for dimensional physical time, conformal time, cycle/stage, and recursive depth.

Where the effective metric branch permits,

\[
d\eta=dt_{\mathrm{phys}}/a_I.
\]

The module generates age from an internal origin event, lookback time relative to a declared observer event, event-time maps, cycle-time maps, and covariance. Recursive depth and stage are never substituted for physical time.

---

## 21. Signal propagation and redshift

On the finite relational carrier, a signal route \(\gamma\) has generated action/delay

\[
\mathcal T_\gamma
=\sum_{e\in\gamma}\tau_e
\]

with edge delays derived from local geometry and propagation law. The continuum limit, when witnessed, gives a ray vector \(k^\mu\) satisfying

\[
k^\nu\nabla_\nu k^\mu=\mathcal F_{\mathrm{nonmetric}}^\mu,
\]

where \(\mathcal F_{\mathrm{nonmetric}}^\mu=0\) on a metric null branch and is explicit otherwise.

For emitter and observer four-velocities \(u_e,u_o\), redshift is defined by

\[
\boxed{
1+z
=\frac{-(u_\mu k^\mu)_e}{-(u_\mu k^\mu)_o}.
}
\]

The relation \(1+z=a_o/a_e\) is recovered only on the appropriate homogeneous, isotropic, comoving, metric, photon-conserving branch.

---

## 22. Jacobi propagation and distance geometry

For metric propagation, the screen-space Jacobi map obeys

\[
\frac{D^2\mathcal D_{AB}}{d\lambda^2}
=-\mathcal R_{AC}\mathcal D_{CB},
\]

where \(\mathcal R_{AC}\) is the optical tidal matrix generated by the realized curvature and matter state.

The angular-diameter distance is

\[
D_A=\sqrt{|\det\mathcal D|}.
\]

With generated photon conversion/loss optical depth \(\tau_{\mathrm{conv}}\),

\[
\boxed{
D_L=(1+z)^2D_Ae^{\tau_{\mathrm{conv}}/2}.
}
\]

Metric propagation and photon-number conservation give Etherington reciprocity:

\[
D_L=(1+z)^2D_A.
\]

Any violation must descend from a nonmetric force, photon conversion/loss, topology, branch structure, or another explicit generated mechanism.

The complete distance registry includes:

- affine and radial distance;
- comoving and transverse distance on branches where defined;
- angular-diameter and luminosity distance;
- geometric distance modulus relative to the internal unit map;
- differential comoving volume;
- volume-averaged distance;
- Alcock–Paczynski combinations;
- lookback time and redshift drift;
- ruler, clock, and siren propagation factors;
- lensing-background kernels;
- derivatives, covariance, branch, and ancestry.

No distance is rescaled to a public ladder.

---

## 23. Isotropic-limit distance identities

On an isotropic metric branch with propagation speed \(c_{\mathrm{prop}}(z)\),

\[
\chi(z)=\int_0^z\frac{c_{\mathrm{prop}}(z')}{H_I(z')}dz',
\]

\[
D_M=S_{k_I}(\chi),
\qquad
D_A=\frac{D_M}{1+z},
\qquad
D_L=(1+z)D_M,
\]

and therefore

\[
D_L=(1+z)^2D_A.
\]

Where the convention is valid,

\[
D_H=\frac{c_{\mathrm{prop}}}{H_I},
\qquad
D_V=\left[zD_HD_M^2\right]^{1/3},
\qquad
F_{\mathrm{AP}}=\frac{D_MH_I}{c_{\mathrm{prop}}}.
\]

These are derived projections of the realized geometry, not survey likelihood objects.

---

## 24. Horizon and causal registry

The primary causal object is the reachability relation generated by the finite signal propagator. On an isotropic branch, a carrier \(X\) with generated speed \(v_X\) has comoving reach

\[
r_X(t_1,t_2)
=\int_{t_1}^{t_2}\frac{v_X(t)}{a_I(t)}dt
=\int_{a_1}^{a_2}\frac{v_X(a)}{a^2H_I(a)}da.
\]

Module I distinguishes:

- Hubble radius;
- particle horizon;
- event horizon when the future integral exists on the declared asymptotic branch;
- apparent, trapped, and anti-trapped structures;
- sound horizon;
- baryon-drag horizon inherited from Module G’s drag surface;
- photon diffusion scale;
- neutrino and relic free-streaming horizons;
- compression-relic interaction/free-streaming horizon;
- dissipative-tail characteristic horizon;
- causal-contact domains.

Every horizon records definition, carrier, speed law, coordinate status, existence conditions, integration/event bounds, uncertainty, branch, ancestry, and downstream use.

No public sound-horizon length enters.

---

## 25. Background derivatives and sensitivities

Let

\[
\dot X_I=\mathcal F_I(X_I;P_{I,\mathrm{in}}).
\]

The tangent map satisfies

\[
\dot J_I
=D_X\mathcal F_I\,J_I+D_P\mathcal F_I.
\]

It generates stable derivatives of expansion, distances, horizons, events, sectors, and Hᵁ coefficient histories.

Where analytic identities are unavailable, derivatives are obtained by tangent/adjoint equations, automatic differentiation, or independently verified numerical differentiation. Uncontrolled differencing of interpolated tables is not accepted.

---

## 26. Uncertainty and covariance

Module I propagates

\[
\boxed{
\Sigma_I
=J_I\Sigma_GJ_I^\mathsf T
+\Sigma_{\mathrm{geom}}
+\Sigma_{\mathrm{sector}}
+\Sigma_{\mathrm{dark}}
+\Sigma_{\mathrm{event}}
+\Sigma_{\mathrm{distance}}
+\Sigma_{\mathrm{interface}}
+\Sigma_{\mathrm{representation}}
+\Sigma_{\mathrm{numeric}}
+\Sigma_{\mathrm{branch}}.
}
\]

For local stochastic/unresolved modes,

\[
\dot\Sigma
=A_I\Sigma+\Sigma A_I^\mathsf T+Q_I,
\qquad Q_I\succeq0.
\]

The covariance includes cross-correlations among sector densities, pressure, transfer, expansion, curvature, clocks, events, horizons, distances, and H[I] coefficient histories. It is not a single uncertainty on one late-time expansion parameter.

---

## 27. Branches, degeneracies, and identifiability

For each admitted parent branch, Module I may produce:

- one unique realized background;
- a no-loss representation-equivalent family;
- multiple physically distinct branches;
- obstruction.

Each branch records initial identity, domain, geometry/topology, sector laws, stability, constraints, asymptotics, events, distances, horizons, covariance, memory, ancestry, and reason for retention or obstruction.

Similar distance maps do not imply physical equivalence. H[I] and Module J may distinguish branches through perturbation response.

No branch is selected because it resembles public data.

---

## 28. Stability, causality, and pathology rules

An accepted branch must exclude or classify:

- negative total or prohibited sector density;
- singular/noninvertible geometry;
- unbounded curvature outside a declared event boundary;
- runaway unwitnessed transfer;
- ghostlike physical quotient modes;
- gradient or sound-speed instability;
- prohibited superluminal propagation;
- loss of hyperbolicity;
- growing unexplained constraints;
- branch chatter or event mislocalization;
- nonmonotonic/multivalued redshift without explicit branch structure;
- unclassified caustics;
- nonphysical horizon behavior;
- numerical stiffness misidentified as physical transition.

A lawful controlled instability is retained only with a bounded domain, event packet, covariance, and downstream warning.

---

## 29. Analytic and accepted-physics limits

The generated system must recover, under their named assumptions:

- static/Minkowski-like geometry;
- radiation-like and matter-like separately conserved expansion;
- curvature-dominated behavior;
- zero-transfer and weak-transfer limits;
- Friedmann-like constraint/acceleration equations as the isotropic continuum reduction of the relational action;
- standard metric null-distance and reciprocity relations;
- low-redshift cosmographic expansions;
- simple anisotropic/curved exact systems.

Limit recovery verifies the construction. It does not import fitted parameters or a public background.

---

## 30. Hᵁ domain compliance

Define the compliance residual vector

\[
\mathfrak R_H(\mathcal B_I)
=(R_{\mathrm{vars}},R_{\mathrm{units}},R_{\mathrm{clock}},
R_{C^1},R_{\mathrm{events}},R_{\mathrm{positive}},R_{\mathrm{bounded}},
R_{\mathrm{species}},R_{\mathrm{sampling}},R_{\mathrm{deriv}},
R_{\mathrm{path}},R_{\mathrm{overlap}}).
\]

The branch is compliant only when every discrete predicate passes and every continuous residual obeys its frozen Hᵁ bound:

\[
\boxed{
\mathcal B_I\in\mathcal D_{\mathcal B}
\iff
\forall a:\ \mathcal P_a(\mathcal B_I)=\mathrm{PASS}
\quad\text{and}\quad
\|R_a\|\le\varepsilon_a^{H^U}.
}
\]

There is no averaging of failed conditions. Failure obstructs H[I]; Hᵁ is not modified post hoc.

The compliance packet includes the frozen operator identity, background identity, complete variable/unit map, event representation, interpolation and derivative representation, domain intervals, covariance, residuals, and hash linkage.

---

## 31. Immutable H[I] activation

After compliance, the only lawful operation is

\[
\boxed{
\mathbb B_{H[I]}
=\operatorname{Instantiate}
\left(\mathbb B_{\mathrm{RFC}}[\mathcal B],\mathcal B_I\right).
}
\]

At coefficient level,

\[
C_H[\mathcal B]\mapsto C_H[\mathcal B_I]
\]

through the frozen insertion map.

Instantiation may populate coefficient histories, units, derivatives, event surfaces, interpolation nodes, and background covariance. It may not change state dimension, equations, gauges, modes, collisions, source grammar, hierarchy, closure, switches, tolerances, uncertainty model, or signatures.

The operator identity is checked before and after substitution. A structural change is an obstruction.

The resulting basis responses and H[I] verification are specified in `science/INSTANTIATED_LINEAR_TRANSFER.md`.

---

## 32. Module I export

For admitted branch \(\iota\), define

\[
\boxed{
P_{I\to H[I]}(\iota)
=(\mathrm{FrozenOperatorIdentity}_H,\mathrm{BackgroundIdentity}_I,
\mathcal B_I,\mathrm{VariableMap}_I,\mathrm{UnitMap}_I,\mathrm{Clock}_I,
\mathrm{Coordinate}_I,\mathrm{Geometry}_I,\mathrm{Curvature}_I,
\mathrm{Topology}_I,\mathrm{Scale}_I,\mathrm{Expansion}_I,
\mathrm{Sector}_I,\mathrm{Transfer}_I,\mathrm{RecombinationBackground}_I,
\mathrm{Event}_I,\mathrm{Horizon}_I,\mathrm{Redshift}_I,\mathrm{Distance}_I,
\mathrm{Sampling}_I,\mathrm{Derivative}_I,\Sigma_I,\mathrm{Branch}_I,
\mathrm{Pathology}_I,\mathrm{Signature}_I,M_I,\mathrm{Anc}_I,
\mathrm{Restart}_I).
}
\]

The Module J-facing background packet contains the same realized geometry, clocks, sector histories, transfer currents, events, horizons, distance geometry, covariance, derivatives, scale/unit ancestry, signatures, branch, memory, ancestry, and restart state.

Module I does not assign primordial mode amplitudes, covariance, tilt, running, phase, cross-correlation, final spectra, growth normalization, nonlinear fields, or empirical likelihoods.

---

## 33. Intrinsic signature registry

Before any public comparison, Module I freezes definitions for:

- compression-relic dilution, pressure, and interaction history;
- dissipative-tail persistence, pressure, transfer, and acceleration contribution;
- sector exchange and equality/crossover structure;
- curvature, anisotropy, topology, and backreaction behavior;
- expansion-derivative and redshift-drift relations;
- horizon ratios and clock-distance consistency;
- reciprocity or explicitly derived violation;
- branch-dependent age/distance relations;
- recursive-memory dependence of background derivatives;
- H[I]-distinguishable background degeneracies.

Each signature records causal derivation, domain, uncertainty, degeneracy, branch, ancestry, explicit falsifier, and freeze identity.

---

## 34. Triad-and-kernel necessity

| Removal or corruption | Necessary consequence |
|---|---|
| remove CIF | no complete geometry, topology, sector-law, transfer, event, horizon, or distance possibility space |
| remove QV | no lawful constraint enforcement, source evolution, branch selection, event localization, or H-domain qualification |
| remove RFL | no stabilized background, restart state, distance/horizon map, signatures, memory, or H[I] packet |
| reorder the triad | possibilities, physical evolution, and stabilized geometry become type-inconsistent |
| remove recursive kernel | relational scale hierarchy, route weighting, refinement, memory, and bounded-tail control are lost |
| remove terminal N-body completion | source averaging, multi-sector exchange, geometry refinement, and signal-route completion are incomplete |
| remove witnesses | geometry, transfers, dark-sector laws, events, or distances can enter without physical support |
| remove compression relic | one generated Big-Implosion sector and its gravitational response are erased |
| replace compression relic with dust | the result becomes a template rather than a derived classification |
| remove dissipative tail | pressure/dissipation ancestry and terminal interface are erased |
| replace tail with a constant | acceleration is inserted rather than generated |
| force flatness/isotropy | curvature, topology, anisotropy, fields, and backreaction are hidden |
| remove early overlap | Module I can overwrite the frozen early universe |
| remove H-domain lock | Hᵁ can be redesigned around the realized background |
| import public expansion/distances | the RFC universe ceases to generate its own background |

---

## 35. Completion and claim boundary

Module I establishes at its declared scope:

- immutable dual-parent consumption;
- explicit CIF/QV/RFL specialization of the enhanced Module A kernel;
- finite relational metric, connection, curvature, topology, averaging, and continuum-carrier construction;
- a generated constrained geometric/sector action and evolution system;
- source, stress-energy, transfer, conservation, and no-double-counting closure;
- internal dimensional constitution and reference events;
- early-to-late continuity with Modules B–G;
- ordinary, radiative, relic, field, and defect background evolution;
- compression-relic and dissipative-tail energy, pressure, transfer, stability, and classification laws;
- generated scale, expansion, acceleration, events, clocks, redshift, signal propagation, distances, reciprocity, horizons, derivatives, covariance, branches, and pathology state;
- proof grammar and realized packet for \(\mathcal B_I\in\mathcal D_{\mathcal B}\);
- immutable H[I] activation and complete Module I export.

Module I does not claim:

- public Hubble, density-fraction, curvature, age, distance, BAO, supernova, siren, or chronometer agreement;
- a measured present-day normalization;
- a fitted cold-dark-matter or dark-energy model;
- primordial perturbation covariance;
- final CMB, matter, lensing, or growth spectra;
- nonlinear structures, stars, galaxies, or feedback;
- terminal Big-Rip dynamics;
- empirical validation.

Within these boundaries, **Module I is complete and frozen** once the companion H[I] instantiation and verification file closes. Module J is then the active child.